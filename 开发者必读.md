# VeroRun AI 开发者必读

> **本地开发辅助文档 · 不进入 Git 版本控制**
> 配套工具：`verorun-dev-insight`（本目录 `repolens.py`）
> 数据快照：基于仓库 `D:\projects\verorun-code` 自动分析结果

---

## 0. 这份文档解决什么问题

VeroRun 是一个**企业级多核 AI 操作系统**：一套平台引擎（**系统核心**）+ 几十个**业务插件**。体量巨大（19 万+ 代码行）且强耦合。

过去，开发者（或 AI 助手）每开发一个功能，都要先通读系统代码才能动手，结果：

- **人** 被海量源码淹没，上手慢、易踩坑；
- **AI** 把大量 token 浪费在重复读取源码上，成本高、上下文还容易被撑爆。

**verorun-dev-insight** 用一条命令把"系统架构 + 插件契约 + 开发规范"提前提炼成结构化报告，开发者直接读报告，AI 直接读 `AI_CONTEXT.md`，**不用再全文读源码**。

---

## 1. 快速开始

工具部署在仓库 `tools/dev_insight/`，是**纯 Python 标准库**脚本（Python ≥ 3.9），零第三方依赖。

```bash
# 方式一：已在仓库 tools/dev_insight 内，自动定位仓库根
python repolens.py

# 方式二：显式指定仓库
python repolens.py --repo D:\projects\verorun-code

# 方式三：自定义输出目录 / 只出某几种
python repolens.py --repo D:\projects\verorun-code --out D:\tmp\insight --only json,md

# 方式四：指定单个插件 / 单个核心模块做深度分析（改前自查用）
python repolens.py --plugin shop          # 深挖 shop 插件：逐文件/类/函数/全部路由
python repolens.py --module plugin_manager # 深挖核心模块
```

**子命令速查（v1.4）**：

| 命令 | 用途 |
|---|---|
| `serve --port 8788` | 启动本地可视化控制台（浏览器点按钮执行；Agent 走 HTTP API，仅绑定 127.0.0.1） |
| `snapshot --save 名字` | 生成基线快照（out/history/<名>.json，可入 Git 供对比） |
| `diff 名字` | 与基线对比：总盘/插件增删/版本/LOC/路由/manifest/依赖变化（--format json 供 Agent） |
| `context --for plugin:shop --depth brief` | 单目标 AI 上下文切片（brief≈2-4KB / normal / full） |
| `query --from plugins --select a.b --where k=v` | 受限查询 JSON（免 grep 大文件） |
| `--deterministic` | 确定性输出：剥离时间/耗时/绝对路径，产物可 diff 可入库 |
| `--fail-on 清单` | 门禁退出码（manifest-invalid/boundary-violation/plugin-cycle/route-unprefixed/file-too-large/all）；`--fail-on-warn` 只报不拦 |
| `--summary-only` | stdout 输出 key=value 指标摘要，不落盘 |

**多仓库（v1.4）**：默认自动发现主仓库同级的 `verorun-workplace` 桌面端仓库并纳入分析（也可 `--extra-repo 路径` 显式指定）；产物顶层追加 `frontend` 段（schema 1.1）。控制台页面即 serve 命令的 UI，Agent 也可直接 POST `/api/analyze` / `/api/snapshot` / `/api/query` 等端点。

**变更热点与依赖图（v1.5）**：全量分析自动附带近 90 天「变更热点」（git churn × 代码行，借鉴 code-maat/CodeScene 方法论）——高风险文件带（churn≥5 且 LOC≥800）即重构优先级清单；报告同时内嵌 Mermaid 插件依赖图（借鉴 madge/dependency-cruiser），支持直接渲染。控制台首页与 HTML 报告均有对应面板/区块，详见《同类工具调研与v1.5增强》。

| 参数 | 说明 |
|---|---|
| `--repo` | VeroRun 仓库根目录（需同时存在 `plugins/` 与 `plugin_manager/`） |
| `--out` | 输出目录，默认 `<脚本目录>/out` |
| `--only` | 逗号分隔的输出类型：`json` `md` `html` `ai`，默认全部 |
| `--plugin <identifier>` | 指定单个插件深度分析（如 `shop`），追加生成 `verorun_deep_<名>.json/.md` |
| `--module <name>` | 指定单个核心模块深度分析（如 `plugin_manager`），同上 |
| `--tree-depth` | 目录树展示深度，默认 `2` |
| `--quiet` | 只输出关键信息 |

**首屏会打印**：核心模块数 · 插件数（含 manifest 待修复数）· 路由数 · 耗时 · 输出文件路径。

---

## 2. 输出物

工具每次运行生成 4 个文件（均不进入 Git）；使用 `--plugin`/`--module` 时额外生成 2 个深挖文件：

| 文件 | 阅读对象 | 说明 |
|---|---|---|
| `repo_lens.json` | 程序 / 深度分析 | 全量结构化信息，机器可读 |
| `repo_lens_report.md` | 人 | 可读的架构报告（含代码量统计，目录、核心、插件表、交互、规范） |
| `repo_lens_report.html` | 人 | 报告的可视化版本（含代码量统计），浏览器直接打开 |
| `AI_CONTEXT.md` | **AI 助手** | token 友好的精简上下文，**开发时把这份给 AI 即可** |
| `verorun_deep_<目标>.json/.md` | 人/AI | 深挖产物：逐文件摘要、类/方法、函数、**完整路由明细**、依赖 |

> **给 AI 的正确姿势**：把 `AI_CONTEXT.md` 作为 AI 助手的上下文输入，替代"让 AI 读整仓源码"。这是本工具减少 AI 资源浪费的核心价值。

---

## 3. VeroRun 系统核心（平台引擎）

报告含**代码量统计**（按语言 / 按顶层目录 / 按文件的代码行 TOP 榜）。

> **统计口径（重要）**：代码行只计真实代码扩展名 `.py/.js/.ts/.vue/.html/.css/.sh/.sql`；已排除 `docs/` 目录、根目录本地调试脚本（`run_final_test*.py`/`debug_*.py` 等）与临时文件；`.md/.yml/.json` 等文档/文案仅计为资产文件数、不计代码行。

以下 17 个模块构成平台引擎（本次实测数据）：

| 模块 | 职责 | .py | 代码行 |
|---|---|---|---|
| **plugin_manager** | 插件框架：发现/加载/生命周期/Hooks/事件总线/技能注册/商店 | 39 | 17,389 |
| **auth-center** | 认证中心：登录、会话、双因素、系统管理 API | 54 | 19,976 |
| **agent_matrix** | Agent 矩阵：系统核心角色（is_system=1）与能力聚合注册 | 16 | 9,874 |
| **orchestrator** | 工作流编排引擎：DAG 节点、调度器、触发分发、安全求值 | 10 | 4,702 |
| **main_site** | 主站服务（Flask 应用）：门户与站点能力 | 8 | 3,135 |
| **admin** | 管理后台服务（Flask 应用）：插件管理、商店、系统设置 | 5 | 1,752 |
| **providers** | Provider 接入层：LLM/模型/外部服务统一适配 | 16 | 1,025 |
| **veroguard** | 授权防护：许可证校验、防篡改 | 15 | 1,821 |
| **sdks** | 对外 SDK | 31 | 2,667 |
| **shared** | 共享基础设施：HTTP 客户端、日志、可观测性 | 6 | 401 |
| **i18n** | 国际化：系统级翻译函数 `_()` 与语言管理 | 3 | 793 |
| health_service | 健康检查服务 | 2 | 123 |
| prompts / static / templates / themes | 提示词与前端资源 | — | — |

**入口脚本**（仓库根）：`auth_server.py`（366 行，auth-center 独立服务）、`health_guardian.py`（270 行，独立看门狗）。

---

## 4. 插件系统契约（开发核心）

### 4.1 插件是什么

插件 = `plugins/<identifier>/` 下的一组文件，通过 `plugin.json` 声明身份与能力，通过继承 `BasePlugin` 接入平台生命周期。**插件能力会聚合到系统核心角色（agent_role），不新建独立 Agent。**

### 4.2 发现规则（与运行时一致）

1. 必须是 `plugins/<name>/` 子目录
2. 必须包含 `__init__.py`
3. 必须包含合法的 `plugin.json`
4. 忽略 `_` 与 `.` 开头的目录（`_base`/`_templates` 是框架资源，非插件）

### 4.3 BasePlugin 必须实现 / 可选实现

所有插件继承 `plugin_manager.base.BasePlugin`。**抽象方法必须实现**：

| 方法 | 阶段 | 说明 |
|---|---|---|
| `setup()` | ENABLED | 插件初始化 |
| `activate()` | ACTIVE | 插件激活（加载模块、注册路由/钩子） |
| `deactivate()` | DISABLED | 插件停用（反注册路由/钩子） |

**生命周期钩子（可选重写）**：

| 方法 | 时机 |
|---|---|
| `on_install(registry)` | 首次安装（适合建数据库表） |
| `on_enable(registry)` | 启用（资源初始化） |
| `on_disable(registry)` | 禁用（资源清理） |
| `on_uninstall(registry)` | 卸载（删表、清配置） |

**能力扩展点（可选重写）**：

| 方法 | 用途 |
|---|---|
| `register_routes()` | 返回待注册的 Flask Blueprints 列表 |
| `register_jobs()` | 返回 APScheduler 任务配置列表 |
| `register_dag_nodes()` | 返回 `{node_type: handler}` 供工作流引擎 |
| `register_health_checks()` | 返回健康检查项列表 |
| `get_event_handlers()` | 返回 `{event_name: handler}` 订阅系统事件 |
| `validate_config()` | 校验 `plugin.json` 的 `config` 段 |

**运行时注入引用**（由 PluginManager 设置）：

- `self.manager` — PluginManager 实例
- `self.app` — Flask 应用（可注册中间件）
- `self.plugin_info` — PluginInfo（清单信息）
- `self._log` — 独立日志器（带插件名前缀）
- `self.t(text)` — 插件自身 i18n 翻译（读 `i18n/{locale}.yml`）
- `self.get_config_value(key)` / `set_config_value(key, value)` — 读/写插件配置

### 4.4 PluginManager 生命周期

```
discover → install → enable(setup) → activate(load+routes) → [active] → disable → uninstall
```

关键 API（全量 43 个方法，详见报告）：`install` / `enable` / `activate` / `disable` / `uninstall` / `mount_active_routes()` / `is_path_allowed(path)`（门卫，判断路径对应插件是否启用）等。

---

## 5. plugin.json 必填与枚举

**必填字段**：`identifier` `name` `version` `description` `author` `min_app_version` `agent_role` `capabilities`

| 字段 | 规则 |
|---|---|
| `identifier` | 唯一标识，必须匹配 `^[a-z0-9_]+$`，≤64 字符 |
| `version` / `min_app_version` | 语义化版本 `X.Y.Z` |
| `agent_role` | **强制**，取 11 个核心角色之一 |
| `capabilities` | **强制**，能力标签列表，非空 |

**agent_role 枚举（11 个）**：`athena` `content` `business` `builder` `finance` `ops` `service` `vision` `creative` `veroscholar` `stock_analyst`

**category 枚举（7 个）**：`system` `shop` `content` `ai_agent` `social` `tools` `supply_chain`

**price_type 枚举（3 个）**：`free` `sub` `onetime`

> manifest 校验由工具自动完成：缺字段、identifier/version 不合规都会在报告中标红。

---

## 6. 核心与插件的交互约定

- **路由**：Flask Blueprint，惯例 `url_prefix=/admin/<identifier>`（如 shop 78 条路由、site_builder 62 条）。
- **数据库**：统一 `plugins/_base/db.py` 的 `get_pooled_connection()` 共享连接池 + 插件独立 schema，**禁止私有连接池**。
- **辅助库**：`plugins/_base/`（`db` / `embeddings` / `ratelimit`）直接导入使用。实测插件侧 `db` 被导入 73 次，`embeddings` 6 次、`ratelimit` 2 次。
- **依赖其他插件**：写在 manifest `depends_on`。
- **插件最常依赖的核心**：`plugin_manager`(135) · `i18n`(91) · `agent_matrix`(59) · `providers`(24)。
- **架构红线**：核心模块**不应直接 import 业务插件**（`plugin_manager` 动态加载除外）。本工具会输出"边界观察项"，本次发现 21 处，建议架构评审关注。

---

## 7. 新插件开发流程（8 步）

1. 复制 `plugins/_templates/react_plugin`（或 `vue_plugin`）到 `plugins/<identifier>/`；
2. 编写 `plugin.json`：8 个必填字段 + `agent_role` 枚举 + `capabilities` 非空；
3. 实现 `BasePlugin` 子类（`setup` / `activate` / `deactivate` 抽象方法）；
4. 用 Flask Blueprint 写路由（`register_routes()` 返回，惯例 `url_prefix=/admin/<identifier>`）；
5. 数据库走 `get_pooled_connection()` + 独立 schema；
6. 文案走插件自带 `i18n/*.yml` + `self.t()`；
7. 运行本工具复核：`python repolens.py`，检查 manifest 校验、路由提取、边界检查；
8. 按 `docs/plugin-standard-v1.7.md`（18 章）与插件审核规范提交审核。

---

## 8. 版本控制约定（重要）

- 本工具（`tools/dev_insight/`）是**本地开发辅助工具，不进入 Git 版本控制**；
- 已在仓库 `.git/info/exclude` 中加入 `tools/dev_insight/`，确保本目录及 `out/` 产物不会被 `git add`；
- 仓库 `README.md` / `README.zh-CN.md` 中已标注该工具为本地工具、无需 commit；
- **不要**把 `out/` 下的报告提交到仓库（属可再生产物）。

---

## 9. 工具维护与扩展（模块化）

```
tools/dev_insight/
├── repolens.py               # 命令行入口
├── repo_lens/
│   ├── config.py            # 仓库定位、排除规则、核心模块知识库
│   ├── fs_scan.py           # 编码安全读取(UTF-8/GBK)、目录树、LOC
│   ├── py_ast.py            # AST 解析：类/函数签名/Blueprint路由/导入
│   ├── core_analyzer.py     # 系统核心 + BasePlugin/PluginManager 契约
│   ├── plugin_analyzer.py   # 插件发现、manifest 校验、路由提取
│   ├── deep_analyzer.py     # 指定目标（插件/模块）深度分析（--plugin/--module）
│   ├── frontend_analyzer.py # 桌面端/前端仓库分析（verorun-workplace 自动发现）
│   ├── server.py            # serve 子命令：本地控制台 UI + Agent HTTP API
│   ├── cache.py             # AST 解析缓存（out/.insight_cache/）
│   ├── gate.py              # 架构门禁（--fail-on 退出码）
│   ├── snapshot.py          # 基线快照与 diff（out/history/）
│   ├── query.py             # 受限结构化查询（query 子命令）
│   ├── interaction_analyzer.py  # 核心↔插件依赖矩阵 + 边界检查
│   ├── standards_extractor.py   # 规范摘要、枚举、文档索引
│   └── report_{md,html,ai}.py   # 三类报告渲染
└── out/                     # 输出目录（git 忽略）
```

**扩展一个新分析维度**：在 `repo_lens/` 新增一个 `xxx_analyzer.py`（实现 `analyze_xxx(cfg, ...) -> dict`），在 `cli.py` 中调用并塞进 `data`，再在 `report_*.py` 加一个区块即可。设计原则：分析器只管"提取结构化事实"，渲染器只管"排版"，互不耦合。

---

## 10. 疑难排查 FAQ

| 问题 | 处理 |
|---|---|
| 报"未找到仓库根目录" | 用 `--repo` 显式指定，或确保存在 `plugins/` 与 `plugin_manager/` |
| 代码行口径怎么算的 | 只计 `.py/.js/.html/.css/.sh/.sql` 等真实代码；排除 docs/、根目录本地脚本与临时文件；md/yml/json 只算资产数 |
| 想看单个插件/模块的完整路由 | `python repolens.py --plugin shop` / `--module plugin_manager`，读 `verorun_deep_*.md` |
| 报告里中文乱码 | 控制台用 GBK 显示 UTF-8 所致；**生成的文件本身是 UTF-8，用编辑器/浏览器打开即正常** |
| 某插件 manifest 标红 | 用报告悬停看到缺失字段；补齐 `agent_role`/`capabilities` 等必填项后重跑 |
| 想只看某个插件 | 读 `repo_lens.json` 中 `plugins.items[]` 对应 entry，或全文搜索 identifier |
| 运行较慢 | 首次约 30s（扫描上万文件 + 数千 AST 解析），属正常；结果会写入 out/ |

---

## 附录：本次运行实测数据快照

- 核心模块 **17** 个 · 业务插件 **37** 个 · 插件路由 **639** 条
- 全仓文本文件 1,753 个 · 代码行约 **281,231**
- manifest 校验：37/37 通过，0 个待修复
- 插件 agent_role 分布：business 8 · service 7 · ops 6 · content 5 · athena 3 · builder 3 · finance 3 · stock_analyst 1 · veroscholar 1
- 代码量 top 插件：stock_analysis(14,436) · site_builder(6,744) · ali_api(5,980) · mini_app_builder(5,895) · health_check(5,853)
- 官方插件模板：React Demo Plugin / Vue Demo Plugin
- 关键文档：`docs/plugin-standard-v1.7.md`（18 章）、`docs/developer-guide.md`、`AGENTS.md`（最高优先级铁律）、`docs/plugin-manifest.schema.json`
