# 代码审计子系统 —— 设计文档与实施手册（策划稿）

> 目标工程：`RepoLucent`（VeroRun 本地开发者工具）
> 版本：v1.0 ｜ 日期：2026-09-12 ｜ 状态：**阶段 1-4 全部实现并通过验收**（内核+规则包+CI注解+serve面板/版本闭环+语义层编排-判定分离；见 §13/§14/§15/§16）。代码审计子系统成型。
> 设计稿 v0.1（旧）
> 定位：把工具从"仓库架构分析器"升级为"专业代码审计工具"。
> 边界：VeroRun 系统只读；实现只落在 `RepoLucent`。审计引擎确定性优先、可复现、零 LLM 为默认（对齐 `serve` 明示的"不接入 LLM"）。

---

## 0. 结论先行

- **不新造审计，而是统一既有三块骨架**：`gate.py`（可插拔 `_check_*` + CHECKS 登记 + `--fail-on`/退出码）、`ci_analyzer.py`（已有 `rule_id + severity{error,warning,info} + summary` 的发现形态）、`scriptlib/dangerous_api_scan.py`（可配置多规则静态扫描引擎）。三者现在各说各话，本方案用**一个 Finding 模型**把它们收敛为同一内核的两个"规则提供方"。
- **规则以数据+代码双形态存在，且挂进刚建的 `registry.json`（新增 `kind:"audit_rule"`）**：每条规则天然可 `list/info/doctor`、可打分、可 `snapshot/diff` 版本化——复用已落地的脚本资产库治理，零重复建设。
- **四维覆盖**：安全 / 架构与规范合规 / 代码质量与坏味道 / AI Agent·业务逻辑。
- **专业性的关键=裁决 + 诚实的覆盖度**：产出**放行/不放行 verdict** 与报告（md/html/json），并入 CI 红线；每条规则标 `coverage: rule_only|semantic`，**verdict 必须显式区分"规则通道零命中"与"已审过且干净"**——绝不因未命中就判通过（这是审计方法论红线）。
- 复用面：`_analyze()` 产出的 `data`（plugins/interactions/overview）作为架构维度规则的现成输入，**不重复扫盘**；报告复用 `report_*`；版本复用 `snapshot/diff`；UI 复用 `serve` 面板 + `/api/*`。

---

## 1. 设计原则（对齐既有基因，逐条给出真实锚点）

1. **加法式扩展、内核零改动**：既有 29 个分析器、`gate.py`、`ci_analyzer.py` 不改；审计引擎通过**调用**它们并**包装其输出**为 Finding 实现。新增仅一个 `vr_insight/audit/` 包 + `cli.py` 的 `audit` 子命令注册 + `server.py` 端点 + 一条 `insight audit` 与 gate 共享退出码。
2. **复用 `_analyze` 的数据**：`gate.evaluate(gates, data, cfg)` 直接吃 `data`（见 `gate.py`），说明架构维度规则无需重扫源码。审计引擎同样以 `data` 为一等输入，源码级规则才走扫描。
3. **复用 `ci_analyzer` 的发现形态**：`{rule_id, severity, …, summary}` 已是审计发现的雏形（`ci_analyzer.py:43-49,89-144`），Finding 模型据此扩展而非另立。
4. **复用 `scriptlib` 当扫描引擎与规则仓**：`dangerous_api_scan` 的"规则=数据、`--rules` 注入"模式（`DEFAULT_RULES`）直接推广成审计规则包；规则登记进 `registry.json` 享受已建的分发/自检/版本闭环。
5. **可复现优先**：默认确定性（AST/regex/结构化数据），同一仓库两次跑逐字节可 diff；语义型规则显式降级为 `coverage:"semantic"` 且默认**不单独支撑放行**，只作提示项，须人工或后续 LLM 层复核。

---

## 2. 统一 Finding 模型（审计内核的数据契约）

`AUDIT_SCHEMA_VERSION = "1.0"`，兼容规则沿用工具的 MAJOR/MINOR 约定。单条 Finding：

```jsonc
{
  "rule_id": "SEC003",
  "dimension": "security",        // security | architecture | quality | ai_business
  "severity": "blocking",         // blocking | major | minor | info（映射旧 CI: error→blocking/major, warning→major/minor, info→info）
  "title": "鉴权 fail-open：密钥缺失时放行",
  "evidence": [{"file":"plugins/x/routes.py","line":188,
                "snippet":"if expected and secret != expected: ...",
                "why":"expected 为空(未配 SECRET)时整段跳过→任意请求视为通过"}],
  "confidence": "high",           // high=确定性命中; medium=启发式需复核
  "coverage": "rule_only",        // rule_only | semantic —— 本结论只来自哪条通道
  "fix": "改为 if not expected or secret != expected: 拒绝；并把空 secret 视为错误配置",
  "references": ["OWASP:A01","内部缺陷 D-11"],
  "provider": "gate|ci|scanner|new",   // 该规则来源
  "source_sha256": "…"                 // 可选：被审文件指纹，供 diff 追溯
}
```

`AuditReport`：
```jsonc
{ "audit_schema":"1.0", "repo":"…", "generated_at":"…",
  "coverage_matrix": {              // ★专业性核心：每维度两通道是否审过、命中多少
    "security":      {"rule_only":{"ran":true,"findings":3}, "semantic":{"ran":false,"reason":"未启用语义层"}},
    "architecture":  {"rule_only":{"ran":true,"findings":1}, "semantic":{"ran":true,"findings":0}},
    "quality":       {"rule_only":{"ran":true,"findings":12},"semantic":{"ran":false,"reason":"未启用"}},
    "ai_business":   {"rule_only":{"ran":true,"findings":2}, "semantic":{"ran":false,"reason":"待人工标注集"}}
  },
  "findings": [ ... ],
  "verdict": {"decision":"not_releasable","blocking":4,"reasons":["security 有 blocking 命中","ai_business 语义通道未审不得视为通过"]},
  "summary": {"by_severity":{...},"by_dimension":{...},"total":18}
}
```

> **红线设计**：`verdict` 计算时，任一维度只要 `semantic.ran==false`，其结论**只能是"规则通道通过"而非"该维度通过"**——把旧 CI/gate"没命中就算过"的隐含假设显式化，避免误放行。

---

## 3. 规则体系：四维 × 来源（首批种子规则）

规则 `kind:"audit_rule"`，声明：`id / dimension / severity / provider(data|scanner|wrap) / detector / fix / coverage`。首批（`★` 为把既有 gate/ci 结论直接包装、零新代码）：

**安全 security**
- SEC001 硬编码密钥/明文凭据（scanner，regex，blocking/high）
- SEC002 SSRF：请求用户可控 URL 未经 host 白名单守卫（scanner 配对模式：`requests.<var>` 与 `_assert_public_host` 同作用域缺失，blocking/medium）
- SEC003 鉴权 fail-open：`expected=env(SECRET); if expected and secret!=expected` 缺 else 拒绝（scanner 配对，blocking/high）
- SEC004 敏感端点未加鉴权装饰器（scanner：`@app.route` 无 `@login_required`/`@_require_admin`，major/medium）
- SEC005 SQL 注入：f-string/`%`/`+` 拼进 `execute(`（scanner，blocking/medium）
- SEC006 免登录端点无限流（scanner 配对，minor/medium）

**架构与规范 architecture（包装既有，零新代码）**
- ARC001 manifest 非法 ★（gate `manifest-invalid`）
- ARC002 核心跨层 import 插件 ★（gate `boundary-violation`）
- ARC003 插件循环依赖 ★（gate `plugin-cycle`）
- ARC004 路由未加 `/admin/<id>` 前缀 ★（gate `route-unprefixed`）
- ARC005 CI 平台合规 ★（包装 `analyze_ci_config` findings）

**代码质量 quality**
- QLT001 超大文件 ★（gate `file-too-large`）
- QLT002 裸异常吞错 `except*: pass`/只 log 不抛（scanner，major）
- QLT003 超长函数/高圈复杂度（AST，minor）
- QLT004 未使用 import / 死局部变量（AST，info）
- QLT005 重复代码块（hash 归一，minor）

**AI Agent / 业务逻辑 ai_business**
- AIB001 异常兜底吞真因（`except: return 通用值`，与 SEC003 互补，major/medium）
- AIB002 配置多源不同步（读多库/两份 config 源，**coverage:semantic**，默认不支撑放行）
- AIB003 prompt 字符串内误用 i18n 包裹（scanner 配对，minor/high）
- AIB004 转人工/关键词规则未落到生效通道（**coverage:semantic**）

> `coverage:semantic` 的规则（AIB002/004 等）第一版**只登记不判定**：输出为"需人工/语义层确认"的提示项，计入 `coverage_matrix` 的 `semantic.ran=false`，确保它们挡住"想当然放行"但不产生误报。

---

## 4. 执行引擎架构（新包，全部加法）

```
vr_insight/audit/
├── __init__.py        # AUDIT_SCHEMA_VERSION
├── model.py           # Finding / Severity / Dimension / Coverage / Verdict / AuditReport（+ 旧 gate/ci→Finding 适配）
├── providers/
│   ├── from_gate.py   # 调 gate.evaluate，把 GateResult→Finding（ARC*/QLT001，零改 gate）
│   ├── from_ci.py     # 调 analyze_ci_config，finding→Finding（ARC005）
│   └── scanners.py    # 源码级规则：以 dangerous_api_scan 的配对引擎为核，跑 SEC*/QLT002-5/AIB001/003
├── registry.py        # 读 registry.json 里 kind=audit_rule 的规则（复用已建注册表）
├── engine.py          # run_audit(repo, dims?, severities?, use_semantic?) → AuditReport
└── report.py          # 渲染 md/html/json（复用 report_html 的样式基元）
```

`engine.run_audit` 流程：解析作用域（哪些维度/规则启用）→ 复用 `_analyze` 拿 `data` → 依次跑 provider（gate/ci/scanner）→ 归一 Finding → 计算 `coverage_matrix` + `verdict` → 交 `report`。`--deterministic` 时剥时间戳，报告可进 Git/可 diff。

CLI：`insight.py audit [--repo R] [--dims security,architecture,...] [--fail-on-severity blocking] [--format md|html|json|all] [--out DIR] [--with-semantic]`。
退出码**沿用 gate 契约**：0=放行 / 1=不放行 / 2=参数环境错 / 3=内部异常。

---

## 5. 裁决模型（verdict）—— 可配置但默认严格

默认"不放行"当且仅当满足任一：
1. 任一 `severity==blocking` 且 `coverage==rule_only` 的命中 ≥ 1；
2. 启用的某维度 `coverage_matrix` 显示"该维度过依赖 semantic 通道且 semantic 未跑"（如只想要 ai_business 结论却没开 `--with-semantic`）→ 判"证据不足，不放行"，不判通过；
3. `--fail-on-severity major` 等收紧阈值时，按阈值判。

`ci-check/audit` 进 CI：`insight audit --fail-on-severity blocking --format json` 的退出码即红线；与既有 `--fail-on` 门禁互补（gate 管架构结构红线，audit 管跨四维的规则包）。

---

## 6. 与既有子系统的关系（复用矩阵）

| 既有资产 | 审计中的复用方式 | 是否改动它 |
|---|---|---|
| `gate.py` | 作为 ARC/QLT 规则的 provider，GateResult→Finding | 否（只调用） |
| `ci_analyzer.py` | ARC005 provider | 否 |
| `scriptlib/dangerous_api_scan` | scanner 规则的配对引擎母版 | 否（复用其 scan()） |
| `registry.json` | 新增 `kind:"audit_rule"` 条目 | 增量（schema 向后兼容，MINOR） |
| `_analyze` / `data` | 架构维度规则的现成输入 | 否 |
| `report_html/md/ai` | 渲染基元与 AI 上下文导出（供 semantic 层） | 否 |
| `snapshot/diff` | 审计报告/规则包打基线、比对回归 | 否 |
| `serve` `/api/*` | 加 `GET /api/audit`、`POST /api/audit/run`（受控） | 增量 |

---

## 7. 落地路线（分期，每期可独立交付+验收）

- **阶段 1（内核+包装，最快见专业报告）**：`audit/model.py`（含 gate/ci→Finding 适配）+ `providers/from_gate,from_ci` + `engine` + `report` 的 md/json；`insight audit` 子命令与退出码。只覆盖 ARC*/QLT001/SEC 里最易确定性化的 SEC001/SEC005。产出带 verdict 的报告。**零新扫描引擎**。
- **阶段 2（源码级 scanner 规则包）**：把 `dangerous_api_scan` 泛化为多规则引擎，落 SEC002/003/004/006、QLT002-5、AIB001/003；规则登记进 `registry.json`（`audit_rule`），`script doctor` 覆盖它们。
- **阶段 3（CI 红线 + serve 审计面板 + 版本闭环）**：`audit` 进 `ci-check` 联动；serve 加审计面板与 `/api/audit*`；`insight audit snapshot/diff` 复用快照做"审计结论随版本演进"回归。
- **阶段 4（语义层，可选）**：导出 `report_ai` 上下文给受控 LLM 层补 AIB002/004 等 `semantic` 规则，**强制标注来源通道、且默认不单独支撑放行**。

---

## 8. 验收与测试计划（断言式，杜绝"假绿"）

- **配对引擎必须正反例双测**：每条 scanner 规则配一个"应命中"样例与一个"逃逸/合规"样例（沿用本工具已验证的 `bad.py/good.py` 手法），断言命中与放过都对。
- **verdict 严格性**：造"规则零命中但 semantic 未跑"的仓库 → 断言 verdict=不放行/证据不足，**不得因未命中判通过**（把审计方法论红线做成可回归断言）。
- **确定性**：同仓库两次 `--deterministic` 跑逐字节相同。
- **零改内核回归**：`--fail-on` 旧门禁、`ci-check`、`serve` 既有 `/api/*` 行为不变；`compileall` 通过。
- **覆盖度矩阵自洽**：`coverage_matrix` 计数 == findings 实际分布；semantic.ran=false 的维度不得出现在"通过"理由里。

## 9. 开放决策点（请评审）

1. Finding 的 `severity` 采用新四级（blocking/major/minor/info）还是直接延用 CI 的 error/warning/info 映射？（本稿建议新四级+映射表，语义更贴"放行"。）
2. 语义层（阶段4）是否纳入本版范围，还是先交确定性内核？
3. 规则包粒度：SEC 类规则是否需要按项目封装命名可配置（如 VeroRun 的 `get_raw_connection`/`_assert_public_host`），以 YAML/JSON 规则文件外置，随仓库走。
4. `audit` 与现有 `gate` 是并存（gate 保留快速结构门禁、audit 做全维报告）还是最终 gate 并入 audit？（本稿建议并存，零改 gate。）

---

## 13. 阶段 1 落地与验收（本轮实际执行）

### 13.1 §9 决策锁定（推荐值）
1. severity 采用新四级 `blocking/major/minor/info`，并提供 CI 三级映射表 `CI_SEV_MAP`（error→blocking、warning→minor、info→info）。
2. 语义层（阶段 4）不纳入本版；`coverage=semantic` 规则只登记不判定。
3. SEC 类规则暂内置于 `scanners.py`，内置默认、预留外置（阶段 2 随仓库以 JSON 规则文件走）。
4. `audit` 与 `gate` **并存**、零改 gate——audit 把 gate 当 provider 包装。

### 13.2 新增/改动文件
| 文件 | 作用 |
|---|---|
| `vr_insight/audit/model.py` | Finding/Dimension/Severity/Coverage/Verdict/AuditReport + CI 映射 |
| `vr_insight/audit/providers/from_gate.py` | 5 个 gate 检查 → ARC001-004 / QLT001（零改 gate） |
| `vr_insight/audit/providers/from_ci.py` | ci_analyzer findings → ARC005（零改 ci） |
| `vr_insight/audit/providers/scanners.py` | 源码级 SEC001/SEC005（复用 fs_scan/CODE_EXTS） |
| `vr_insight/audit/engine.py` | 编排 + 覆盖度矩阵 + 放行裁决（证据不足不放行） |
| `vr_insight/audit/report.py` | md/json 渲染（裁决置顶） |
| `vr_insight/cli.py` | `audit` 子命令 + `_cmd_audit` + handlers 注册（加法式） |
| `vr_insight/registry.json` | 新增顶层 `audit_rules`（10 条，含 2 条 semantic 登记） |

### 13.3 用法
```
python insight.py audit --repo <VeroRun仓> [--dims security,architecture,quality,ai_business]
                        [--fail-on-severity blocking] [--format md,json] [--deterministic] [--with-semantic]
```
退出码：0=放行 / 1=不放行（与 gate 同契约，可直接进 CI）。报告落 `out/audit/audit_report.{md,json}`。

### 13.4 验收矩阵（实跑）
| 用例 | 结果 |
|---|---|
| 端到端 | 对 `tests/fixture_repo` 跑通：gate 的 manifest 失败被包成 `ARC001/blocking`、出覆盖度矩阵、裁决不放行、rc=1、报告落盘 ✅ |
| 证据不足红线 | `--dims ai_business`（无确定性通道且语义未跑）→ 裁决"不放行·证据不足"，**未因零命中判通过** ✅ |
| 确定性 | 同仓两次 `--deterministic` json 报告 sha256 逐字节相同 ✅ |
| 扫描规则非死规则 | SEC001 命中硬编码密钥/放过 env 与 trivial；SEC005 命中 f-string/拼接/放过参数化查询 ✅ |
| 零改内核回归 | `compileall vr_insight` 通过；`script doctor`、既有子命令不受影响 ✅ |
| 注册表合法 | registry JSON 合法，scripts=10、audit_rules=10 ✅ |

### 13.5 已知边界（诚实声明，阶段 2+ 补）
- 阶段 1 源码扫描仅 SEC001/SEC005 两条；SSRF/fail-open/未鉴权端点/限流（SEC002-004/006）、质量类 QLT002-005、AI 类 AIB001/003 在阶段 2 落。
- 语义型规则（AIB002/004 配置多源、转人工生效通道）当前只进 `coverage_matrix` 的 `semantic.ran=false`，用于**挡住"想当然放行"**，不产生 finding。
- serve 的审计面板与 `/api/audit*`、CI 联动、`audit snapshot/diff` 版本闭环在阶段 3。

---

## 14. 阶段 2 落地与验收（本轮实际执行）

### 14.1 核心：把源码规则升级成"数据驱动 + 可外置"的规则包引擎

新增 `vr_insight/audit/providers/rulepack.py`：规则即数据，`scanners.py` 改为消费它。三类匹配器全确定性：
- `regex_line`：逐行正则 + 可选 `guard_anywhere`（文件含守卫名则整条跳过，用于 SSRF 降噪）；
- `ast`：结构化检查 `bare_except / except_return_fallback / long_function / unused_imports`；
- 外置：`load_repo_rules()` 读仓库 `audit_rules.json`/`.verorun/audit_rules.json`，CLI `--audit-rules FILE` 追加 → **新增规则零改代码、随仓库走**（兑现 §9-3）。

### 14.2 阶段 2 新增规则（12 条确定性规则中新增 6 条）
| 规则 | 维度/级别 | 匹配器 |
|---|---|---|
| SEC002 SSRF（外发 URL 缺主机守卫） | security/major | regex_line + guard_anywhere |
| SEC003 鉴权 fail-open（`if secret and …`） | security/major | regex_line |
| QLT002 吞异常 `except:pass` | quality/minor | ast:bare_except |
| QLT003 超长函数（≥80 行） | quality/minor | ast:long_function |
| QLT004 未使用顶层 import（`__all__` 豁免） | quality/info | ast:unused_imports |
| AIB001 兜底吞真因（except 返回常量且未记录异常） | ai_business/major | ast:except_return_fallback |

`ai_business` 现有了确定性规则 → 纳入默认四维；`audit_rules` 登记由 10 增至 16。engine 编排改为按维度请求触发 providers（scanners 一次覆盖三维）。

### 14.3 验收（实跑）
| 用例 | 结果 |
|---|---|
| 正反例单元自证 | 6 条规则"该命中/该放过"全对：SSRF 带 `_assert_public_host` 放过；`except as e: log(e)` 不判吞异常；短函数/`__all__` re-export 放过；except 记录异常不判 AIB001 ✅ |
| 真实管线命中 | fixture 审计 `QLT004` 命中 `plugin_manager/manager.py:1` 未用 import；四维覆盖矩阵、裁决不放行(rc=1) ✅ |
| 外置规则注入 | `--audit-rules` 传 `CUSTOM_R1(匹配 Blueprint)` → 命中 `plugins/demo/routes.py:2`；坏 JSON 优雅 rc=2 ✅ |
| 确定性 | `--deterministic` 两次 json sha256 相同 ✅ |
| 零改内核回归 | `compileall` 通过；`script doctor`、既有子命令不受影响；gate/ci 本体未动 ✅ |

### 14.4 本轮修复
- `rulepack._a_unused_imports` 一处死分支清除；`from_gate/from_ci` 相对导入需三点（`from ... import gate`）已修；`--audit-rules` printf 转义坏 JSON → 由 CLI 优雅报 rc=2（非缺陷，异常路径正确）。

### 14.5 诚实标注：阶段 2 仍未覆盖
- 安全 SEC004（敏感端点缺鉴权装饰器）、SEC006（匿名端点无限流）因高误报风险未做纯静态版（留配对/上下文判定，阶段 2.5 或 semantic）。
- 质量 QLT005（重复代码块）未做（需指纹算法，收益/成本比低，暂缓）。
- ai_business 的 AIB002/AIB004（配置多源、转人工生效通道）仍为 `semantic` 登记、不判定 → 靠 §13 的"证据不足不放行"守住，不被误判为已审。

---

## 15. 阶段 3 落地与验收（CI + serve 面板 + 版本闭环）

### 15.1 改动
| 文件 | 变更 |
|---|---|
| `audit/versioning.py`（新） | 审计结论"确定性指纹"存基线（`out/history/audit/`，复用 `snapshot` 的 `_safe_name/_dumps/history_dir`）+ 基线 diff + `render_diff_md` |
| `audit/report.py` | 新增 `render_annotations`：GH/Gitee `::error/::warning/::notice file=,line=` + 裁决行 |
| `cli.py` | `audit` 加 `--format annot`、`--save-baseline`、`--diff-baseline`；`_cmd_audit` 写文件/打注解/存比基线 |
| `server.py` | 只读 `GET /api/audit`（规则目录+上次结论）；受控 `POST /api/audit/run`（confirm 门控、仅审 serve 当前仓库、缓存 `last_audit`）；控制台「🛡️ 代码审计」面板 + 预览/执行 JS；API 文档同步 |

### 15.2 用法
```
# CI 红线：把审计注解打到构建
insight.py audit --repo <仓> --format annot --fail-on-severity blocking   # 退出码 0放行/1不放行即红线
# 版本闭环：先存基线，改动后对比
insight.py audit --repo <仓> --save-baseline v1
insight.py audit --repo <仓> --diff-baseline v1
# 浏览器：python insight.py serve → 「🛡️ 代码审计」→ 预览(dry-run) / 执行(confirm)
```

### 15.3 验收（实跑）
| 用例 | 结果 |
|---|---|
| CI 注解 | `--format annot` 输出 `::error file=Bad-Id::[ARC001]…`、`::notice …QLT004`、末行裁决；rc=1 ✅ |
| 基线无变化 | `--save-baseline base-a` 后 `--diff-baseline base-a` → 裁决不变、无增减 ✅ |
| 基线真实差异 | 用 `--dims quality` 比 `base-a`（四维基线）→ 显示 裁决翻转、`architecture/ARC001` 消失、`blocking 1→0` ✅ |
| serve dry-run | `POST /api/audit/run {confirm:false}` → 返回 target/dims/fail_on，不执行 ✅ |
| serve 执行 | `{confirm:true}` → 跑分析、返回完整报告（不放行/阻断1/四维覆盖），并缓存 `last_audit`，再 `GET /api/audit` 见缓存 ✅ |
| serve 只读边界 | 仅审 serve 当前 `--repo` 指向仓库，不接受任意路径入参 ✅ |
| 回归 | 编译通过；`script doctor`、`/api/scripts`、既有子命令不受影响；`gate`/`ci` 本体未动 ✅ |

### 15.4 本轮修复
- `audit/versioning.py` 引 `snapshot` 需两级 `from .. import snapshot`（初写单点报 ImportError），已修。

> 边界确认：serve 审计测试以 dev 工具自带 `tests/fixture_repo` 作 `--repo`（不含 VeroRun 产品代码）；审计全程只读源仓，注解/报告/基线只写 `out/`。

### 15.5 剩余（阶段 4）
- 语义层：导出 `report_ai` 上下文，对 `AIB002/AIB004` 等 `coverage:semantic` 规则做受控增强，强制标注来源、默认不单独支撑放行；并把审计面板接上"维度多选/规则勾选"与 `--audit-rules` 在线注入。

---

## 16. 阶段 4 语义层落地与验收

### 16.1 设计：编排—判定分离（工具仍不接 LLM）
`serve` 明示"不接入 LLM"，故语义层不内嵌模型，而是：
- **导出** `audit/semantic.py: build_prompt()` → 生成"语义审计任务书"（待判定规则 + 严格输出 schema + 复用 `report_ai.render_ai_context` 的仓库 AI 上下文），交外部 AI Agent 判定；
- **回灌** `load_results()/load_items()` → 校验 Agent 结果（rule_id 必须在 semantic 目录、必须带 file 证据，否则丢弃并入 `rejects`），转 `Finding(coverage=semantic, provider=semantic)`；
- **红线**：语义发现默认是**咨询项**，不纳入阻断（`run_audit(semantic_can_block=False)`）；只有显式开启才影响放行。语义通道只会让结论更严，绝不清白化规则通道结论。回灌后该维度 `coverage.semantic.ran=true`。

`SEMANTIC_RULES` 目录：AIB002（配置多源）、AIB004（规则未落生效通道）、SEC004（敏感端点缺鉴权）——均"需跨文件/上下文理解"，故只走语义。

### 16.2 用法
```
# CLI：导出任务书 → 交 Agent → 回灌（默认咨询）
insight.py audit --repo <仓> --emit-semantic prompt.md
insight.py audit --repo <仓> --semantic-results agent_findings.json            # 仅展示、不改放行
insight.py audit --repo <仓> --semantic-results agent_findings.json --semantic-can-block --fail-on-severity major
# serve：面板「导出语义任务」+ 粘贴结果 textarea + 「带语义执行」(可勾选纳入阻断)
```

### 16.3 验收（实跑）
| 用例 | 结果 |
|---|---|
| 导出任务书 | `--emit-semantic` 生成 100 行 md，含"待判定规则/输出 schema/AI 上下文"三段 ✅ |
| 回灌=默认咨询 | `--semantic-results`（1 合法+1 未知）：`ai_business` 语义通道 ✅ 已审、AIB002 标 `语义/咨询`，但**裁决仍 releasable**（major 不被咨询计入）；未知规则 `NOT_A_RULE` 被拒并 stderr 提示 ✅ |
| 可阻断翻转 | `--semantic-can-block --fail-on-severity major` → 裁决 not_releasable、阻断 1 ✅ |
| serve 端点 | `GET /api/audit/semantic-prompt`→任务书；`POST /api/audit/run` 带 `semantic_results` 默认放行、`semantic_can_block:true` 翻不放行 ✅ |
| 回归 | 编译通过；`script doctor`、`/api/*`、`audit` 既有维度不受影响；`gate`/`ci`/分析器本体未动 ✅ |

### 16.4 本轮修复（均由运行暴露后修复并复测）
- `audit/semantic.py` 相对导入：`Finding` 等同包 model 应 `from .model`（误写 `..model` 指向不存在的 vr_insight.model）；
- `build_prompt` 的 `rules` 需保留 `(rid, r)` 二元组以匹配 `for rid, r in rules`（原写成只含 dict 触发解包异常）。

### 16.5 系统收尾
代码审计子系统四阶段全部落地：**统一 Finding 内核（gate+ci+扫描 三源收敛）→ 数据驱动可外置规则包 → CI 注解 + serve 审计面板 + 审计基线 diff → 语义层编排-判定分离**。全程零改 `vr_insight` 分析器/`gate`/`ci` 本体，确定性优先、可复现、LLM 不内嵌，且以"覆盖度矩阵 + 证据不足不放行 + 语义只加严"三条红线保证专业性。
