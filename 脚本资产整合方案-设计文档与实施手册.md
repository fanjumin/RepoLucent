# AI 生成脚本资产整合方案 —— 设计文档与实施手册

> 目标工程：`RepoLucent`（VeroRun 本地开发者工具，主包 `vr_insight`，入口 `insight.py`）
> 版本：v1.3 ｜ 日期：2026-09-12 ｜ 状态：**阶段 A+B+C 全部实现并通过验收**（7 个 active 脚本；§10 阶段A / §11 阶段B / §12 阶段C+UI）。CLI 与 serve（只读端点 + 受控 run）均已落地。
> 边界铁律：**VeroRun 系统（各智能体工作空间内的产品代码）只读，不得写入；一切交付物只落在 `RepoLucent` 内。**

---

## 0. 结论先行（给评审者 30 秒）

- 本地 24 个智能体工作空间共约 1001 个 `.py`，**真实"AI 开发期生成脚本"经去噪为 249 个**；其中 111 个绑定内网地址、仅 33 个纯标准库、21 个进入"高复用四维分"短名单。
- 绝大多数脚本是**绑定某次 VeroRun 审计的一次性探针**，直接入库会污染工具。**只提炼 6 个可参数化的通用能力**进脚本库（Tier A），其余作"模式借鉴/归档溯源"。
- 接入形态：**注册表 + 新增 `insight.py script <动作>` 子命令**，复用工具既有的 `run()→handlers` 分发、`snapshot/diff` 版本对比、`TOOL_VERSION/SCHEMA_VERSION` 兼容契约、报告渲染等地基。**对既有 29 个分析器内核零改动**，仅在 `cli.py` 增加 2 行加法式注册。
- 本文档的**普查与打分环节已实际运行验证**（见 §2、附录 A 的断言数与产出）；**落地代码为【参考实现】**，评审通过后再实施。

---

## 1. 设计原则（对齐工具既有基因）

在动任何设计之前，先读了工具的真实源码，三条结论决定了整套方案的形态：

1. **工具早已"认识"这批脚本。** `vr_insight/config.py` 中已有
   `ROOT_LOCAL_SCRIPT_PREFIXES = ("verify_","check_","debug_","test_","find_","get_","inspect_","deploy_patch",...)`，
   并把它们**排除出统计**。也就是说"开发者本地一次性脚本"这个概念在工具里是一等公民——本方案不是新增范式，而是给这个既有类别补上"发现→编目→按需调用"的闭环。

2. **分发是加法式注册表。** `cli.py:run()`（约 1049 行）用
   `handlers = {"snapshot":_cmd_snapshot, "diff":_cmd_diff, ..., "ci-check":_cmd_ci_check}` 做命令到函数的映射。新增子命令 = 往这个 dict 加一个键 + 在 `_build_argparser()` 加一个 `sub.add_parser(...)`。既有子命令一行不动。

3. **版本契约现成可复用。** `__init__.py` 有 `TOOL_VERSION="1.5.0"` 与 `SCHEMA_VERSION="1.2"`，并写明兼容规则（MAJOR 删/改名/改类型；MINOR 只增可选字段，消费方须忽略未识别字段）。`snapshot.py` 有 `build_snapshot/save_snapshot/load_snapshot/list_baselines/diff_snapshots/render_diff_md/default_name` 与 `SNAPSHOT_VERSION`。脚本注册表的版本管理直接**复用这套语义**，不重造轮子。

> 这三点是"**复用工具既有地基、内核零改动、不重造轮子**"的落点（注：本平台红利一词本指 VeroRun 插件平台的扩展缝，此处是独立工具复用自身模块，不宜混用）：本方案 95% 是**编排既有能力**，新增的只是一个数据文件 + 一个分发函数。

---

## 2. 任务①② —— 扫描与筛选（已运行验证）

### 2.1 扫描机制

普查器 `inventory.py`（见附录 A，**已实际运行**）遍历工作空间根目录，对每个 `.py` 抽取**客观复用信号**，而非靠文件名臆断：

- 规模（行数）、是否有 `__main__`、是否可参数化（`argparse/click/fire/sys.argv`）；
- 顶层 import 集合 → 判定"仅标准库 / 需第三方 / 需内网服务"；
- 环境耦合信号：硬编码绝对路径计数、内网 IP/host/URL 计数、DB 驱动、明文凭据；
- AST 解析函数/类数、模块 docstring 首行（功能摘要）。

**去噪口径**（把"产品代码"与"AI 开发脚本"分开）：排除 `uploads/ .venv/ __pycache__/ site-packages/ .git/ fixture*/ node_modules/`，排除整仓 checkout 目录（`wt-*/ verorun-code/` 等，它们含 admin/main_site/plugin_manager 等产品目录），排除工具自身 `vr_insight`。

### 2.2 筛选漏斗（真实数字）

| 阶段 | 数量 | 口径 |
|---|---:|---|
| 全量 `.py`（含整仓 checkout） | 1001 | 24 工作空间未去噪 |
| **去噪后候选脚本** | **249** | 产品代码/依赖/夹具已剔除 |
| 绑定内网 host/IP | 111 | 环境耦合，复用性差 |
| 纯标准库依赖 | 33 | 无第三方、可离线 |
| 含 `__main__` 可独立跑 | 37 | — |
| 有 `argparse/click/argv` 可参数化 | 14 | 高复用关键信号 |
| AST 语法错误 | 2 | 直接排除 |
| **四维分≥4 去重短名单** | **21** | 进入精读 |

### 2.3 四维评分模型

`score.py`（已运行）对每个候选在四维打分、加总排序，短名单再经人工精读定档：

- **功能价值 V**：规模适中(40–400 行)且结构化（≥2 函数）者更可能有普适价值。
- **代码质量 Q**：语法通过 / 有模块 docstring / 有 `__main__`。
- **可复用性 R（核心权重）**：无内网 host(+2)、依赖仅标准库(+2)、可参数化(+1)、无硬编码绝对路径(+1)。
- **集成成本 C（反向）**：无网络/无 DB/无明文凭据各 +1（越低越易安全接入）。

精读结论（用真实 Read 逐行核对，非猜测）：短名单里真正的**通用能力**只有少数几件，且**价值在"抽象后的模式"**——原始脚本多为一次性。据此分级：

- **Tier A｜一等可复用（提炼参数化后入库，6 项）**：见 §4。
- **Tier B｜模式借鉴（抽为测试/验证基建基类）**：离线 DB 桩、子进程 JSON-RPC 往返 harness（与 A 合表管理，标注 `kind: harness`）。
- **Tier C｜一次性归档（登记溯源，不入库）**：`build_plan / copy_patchset / verify_digest / verify_substrate_changes / sa_a1a4_newcode / db_forensics / market_calendar` 及大量 `verify_* / test_*` 审计脚本。它们**绑定特定结论/仓库**，入库即污染，仅保留"可借鉴手法"。

> 严谨性说明：`market_calendar` 虽通用但属**行情业务域**，与"代码分析工具"跑题，判不入库；`sloc / bloat` 与工具现有 `overview` LOC 统计**存在能力重叠**，定位为"轻量速查/时间窗归因"增量，并在元数据 `overlap` 字段显式标注，避免团队误用双份口径。

---

## 3. 任务③ —— 分类体系（功能模块）

按"工具能拿去干什么"分 8 类，作为注册表 `category` 的取值域与 `script list --category` 的过滤维度：

| category | 含义 | Tier A/B 成员 |
|---|---|---|
| `analysis` | 静态代码/仓库分析 | dangerous_api_scan, sloc_summary, git_churn, script_sweep |
| `verification` | 断言/校验（读真实文件核事实） | anchor_assert |
| `testing` | 测试基建（离线替身/往返） | offline_db_stub, subprocess_harness |
| `git-workflow` | 版本库增量/历史 | git_churn（与 analysis 双挂，主归此） |
| `reporting` | 报告/看板生成 | （归档参考，暂无入库） |
| `ops` | 交付搬运/部署辅助 | （仅归档参考） |
| `data` | 数据/DB 查询取证 | （仅归档参考） |
| `util` | 通用小工具 | （分域后视情况） |

分类落地为注册表字段的**受控枚举**（非自由文本），保证 `list/info` 可稳定过滤。

---

## 4. 任务④ —— 元数据 Schema 与入选脚本元数据

### 4.1 元数据 schema（`SCRIPT_META_VERSION = "1.0"`，兼容规则同 §5）

每条脚本在 `registry.json` 中的字段：

```jsonc
{
  "id": "dangerous_api_scan",         // 唯一短名，= 子命令调用名 script run <id>
  "name": "危险 API 调用静态扫描",
  "category": "analysis",             // §3 受控枚举
  "kind": "tool" | "harness",         // tool=可直接跑；harness=需二次填充的测试模板
  "summary": "扫描仓库中禁用调用模式（如裸连接被直接 execute），退出码 0/1 可做 CI 红线。",
  "entry": "scriptlib/dangerous_api_scan.py",   // 相对 vr_insight 包，参数化后的实现
  "origin": {                          // 溯源：从哪个 AI 脚本提炼（只读，不改它）
    "source_path": "<workspace>/mtvjkbol.../scan_raw_conn_misuse.py",
    "source_sha256": "…", "extracted_at": "2026-09-12"
  },
  "inputs": [                          // 参数契约（供 info 展示与 doctor 校验）
    {"name":"--root","type":"path","required":false,"default":".","desc":"扫描根目录"},
    {"name":"--rules","type":"file","required":false,"default":"builtin","desc":"禁用规则清单(YAML)"}
  ],
  "outputs": {"stdout":"命中清单(文件:行:片段)","exit":"0 无命中 / 1 有命中 / 2 参数错"},
  "deps": {"stdlib_only": true, "third_party": [], "network": false, "needs_repo": false},
  "reuses_platform": ["fs_scan.iter_repo_files", "py_ast.parse_python_file"], // 享受的地基
  "usage": "insight.py script run dangerous_api_scan --root <repo> --fail-on-hit",
  "overlap": "",                        // 与既有能力的重叠说明（如 sloc vs overview），无则空
  "notes": "规则需按项目封装命名配置；未配置封装时给出通用兜底集。",
  "version": "1.0.0",                   // 脚本自身 semver，见 §5
  "status": "active" | "deprecated" | "archived",
  "tags": ["ast","ci","lint"]
}
```

### 4.2 Tier A/B 入选脚本元数据（精炼摘要，完整随 `registry.json` 交付）

**A1 `dangerous_api_scan`**（analysis ｜ tool ｜ stdlib ｜ **高**）
- 提炼自 `scan_raw_conn_misuse.py`（真实精读：AST 找"裸 `get_raw_connection()` 被直接 `.execute()`"，argv 传目录，退出码 0/1）。
- 参数化改造：把"封装名+禁用调用"从硬编码改为 `--rules` YAML；内置通用兜底规则集。复用 `fs_scan.iter_repo_files` + `py_ast`。
- 输出命中清单 + 非零退出，直接接 CI。`overlap`：与 `gate.py` 的既有门禁项并列（互补，非替代）。

**A2 `sloc_summary`**（analysis ｜ tool ｜ stdlib ｜ **中**）
- 提炼自 `sloc.py`（cloc 风格 code/comment/blank 按语言+顶层模块，排除依赖/构建目录）。
- 改造点：去除硬编码 `ROOT`，改 `--root`；**显式标注 `overlap`：与 `vr_insight` 既有 `overview` LOC 口径可能分叉**，定位为"任意目录轻量速查"，非 VeroRun 全仓统计（后者仍用主命令）。

**A3 `git_churn`**（git-workflow ｜ tool ｜ stdlib+外部 git ｜ **中**）
- 合并 `bloat_analysis.py` + `bloat_recent.py`（git numstat 按时间窗/目录/插件净增归因，定位膨胀源）。
- 改造：`--repo --since --window --group-by {dir,plugin,file}`；两件重叠合一，避免双份。

**A4 `anchor_assert`**（verification ｜ tool ｜ stdlib ｜ **高·方法论核心**）
- 提炼自 `anchors.py`（读真实文件→逐条断言"报告引用的改动锚点原文仍在"→PASS/FAIL+退出码）。**注意**：原脚本 100+ 锚点全硬编码＝一次性；**抽象后极通用**——正好把"引用真实文件→断言锚点存在→（可选）打同款补丁"这一审计方法论做成团队可复用工具。
- 改造：锚点清单外置为 YAML（`{file, must_contain:[...], expect_line?}`），`script run anchor_assert --against <repo> --claims claims.yaml`；`--patch` 时可选应用补丁并复跑断言。

**B1 `offline_db_stub`**（testing ｜ harness ｜ stdlib ｜ 基建）
- 提炼自 `chatbot/tests/_stubs.py`（内存 dict 假装 SQL 语义 + `install_stub` monkeypatch 被测模块的 `_db()`，不连真库）。
- 形态：可 `import` 的测试替身基类，参数化表结构/语句；作 `harness` 供离线单测复用。

**B2 `subprocess_harness`**（testing ｜ harness ｜ stdlib+numpy ｜ 基建）
- 提炼自 `test_mcp_roundtrip.py`（起子进程跑 server，JSON-RPC 验 stdio 往返，含"无 DB 无网络时优雅降级"断言）。
- 形态：往返协议验证模板，工具名/期望值参数化。

> Tier C 全部登记为 `status: archived`（`kind: tool`，`entry: null`，只存 `origin` 与一句话摘要），供"下次遇到同类需求先查有没有现成参考"，但 `script run` 会拒绝并提示"一次性归档、请走 `script sweep` 重新提炼"。

---

## 5. 任务⑤ —— 调用机制（注册表 + 新子命令，零改内核）

### 5.1 目录与数据布局（全部落在开发者工具内）

```
vr_insight/
├── script_cmd.py          # 新增：_cmd_script 二级分发（唯一新增可执行逻辑）
├── scriptlib/             # 新增：提炼参数化后的 Tier A/B 脚本（一等模块）
│   ├── __init__.py
│   ├── dangerous_api_scan.py
│   ├── sloc_summary.py
│   ├── git_churn.py
│   ├── anchor_assert.py
│   ├── offline_db_stub.py
│   └── subprocess_harness.py
└── registry.json          # 新增：脚本注册表（元数据唯一事实源）
```

### 5.2 子命令契约

```
insight.py script list [--category analysis] [--json]     # 列出脚本库（复用报告渲染风格）
insight.py script info <id>                               # 打印某脚本完整元数据（含 usage）
insight.py script run <id> [脚本自有参数 ...]             # 调度执行（透传 argv，返回其退出码）
insight.py script sweep <root> [--min-score 4] [--json]   # 复用普查器扫新脚本，输出候选建议（不改源文件）
insight.py script doctor [<id>]                           # 逐条冒烟自检：import+跑内置 smoke，PASS/FAIL+退出码
```

### 5.3 对 `cli.py` 的**唯一**改动（2 行加法，既有分析器零触碰）

```python
# cli.py :: _build_argparser() 内，与 snapshot/diff 等同级
scp = sub.add_parser("script", help="脚本资产库：编目/调用/普查/自检 AI 生成脚本")
scp.add_argument("action", choices=["list","info","run","sweep","doctor"])
scp.add_argument("rest", nargs=argparse.REMAINDER)  # 原样透传给脚本

# cli.py :: run() 的 handlers dict 增一键
handlers = {..., "script": _cmd_script}
```

`_cmd_script` 内部按 `action` 再分发；`run <id>` 时从 `registry.json` 取 `entry`，用 **importlib 动态导入 `scriptlib` 模块并调用其 `main(argv)`**（不 exec 任意路径、不接受注册表外的 id → 天然防注入、边界清晰）。`sweep` 直接调 `scriptlib` 内置的普查函数。**这是加法式扩展点，不修改任何 `_cmd_analyze/_cmd_snapshot/...` 与 29 个分析器。**

> 【参考实现】`script_cmd.py` 与 `scriptlib/*.py` 的完整代码在评审通过后落地；其**骨架已在本轮以 `inventory.py`/`score.py` 形式实跑验证**（附录 A）。

### 5.4 安全与边界

- 只执行 `registry.json` 白名单内、位于 `scriptlib/` 的脚本；`sweep` 对源工作空间**只读**，绝不写回 VeroRun 系统。
- 脚本继承工具的错误契约：`run()` 顶层已把异常归一为 `{error:{type,message,hint}}` 写 stderr、退出码 1=门禁/2=参数/3=异常；`script run` 复用之。

---

## 6. 任务⑥ —— 版本管理与更新机制（复用 snapshot/version 地基）

### 6.1 三级版本（全部对齐工具既有语义，不新增体系）

1. **`TOOL_VERSION`**：脚本库随工具发布，属工具一部分，不单独发版。
2. **`SCRIPT_META_VERSION`（= `registry.json` 的 schema 版本，起 `1.0`）**：沿用 `__init__.py` 已写明的兼容契约——
   - MAJOR：删字段/改字段类型或语义（如把 `inputs` 由数组改为 map）→ 须同步改 `script_cmd` 与文档；
   - MINOR：新增可选字段（如加 `provenance.url`）→ 消费方忽略未识别字段即可。
3. **每条脚本 `version`（脚本自身 semver）** + `status: active|deprecated|archived`。

### 6.2 变更可 diff：直接复用 `snapshot.py`

- `insight.py script list --json > out/history/...`（或新增 `script snapshot <name>`）→ 调 `save_snapshot` 把"当前注册表指纹"存基线；
- 注册表被增删改后 `insight.py script diff <baseline>` → 复用 `diff_snapshots/render_diff_md`，产出"新增了哪些脚本、哪些 deprecate、version 变化"的人读变更报告。
- 快照指纹字段建议：`{id, version, status, category, origin.source_sha256}` 的有序聚合（稳定可 diff）。

### 6.3 更新/迭代闭环（团队日常）

1. AI Agent 又产出同类脚本 → `insight.py script sweep <workspace>` 给出候选与四维分；
2. 值得沉淀者：提炼参数化 → 放入 `scriptlib/` → 在 `registry.json` 加/改条目（脚本 `version` 递增、填 `origin.source_sha256`）；
3. `insight.py script doctor <id>` 冒烟自检（import + 内置 smoke）通过才置 `active`；
4. 需要退役者置 `deprecated→archived`，**不物理删除**（保留溯源），与工具"发布=提升不清场"的既有铁律一致。
5. 提交前 `script snapshot` 打基线，`script diff` 看回归。

---

## 7. 任务⑦ —— 团队使用文档（随交付物产出）

计划随代码一起交付两份文档（本轮先出骨架，实现轮补全）：

- **《脚本库使用手册》**：`script list/info/run` 典型工作流；每个 Tier A 脚本的"输入/输出/依赖/示例/注意事项"（取自 §4 元数据，单一事实源＝`registry.json`，手册可 `script list --md` 自动生成，避免文档与实现分叉）。
- **《脚本沉淀规范》**：什么该入库（Tier 判据）、如何参数化（禁硬编码路径/内网 host/明文凭据，须 `argparse`、须 stdlib 优先、须带 `--help` 与 smoke）、如何登记版本与打基线、`sweep→doctor→snapshot` 三步闭环。
- 集成进工具现有的《开发者必读》目录（同目录已有 `开发者必读.md`），并在 `config.py` 的"扩展指引"注释处补一行指向脚本库。

---

## 8. 落地路线（评审通过后分阶段）

- **阶段 A（低风险、纯增量）**：建 `scriptlib/` + `registry.json`；迁移 A1 `dangerous_api_scan`、A4 `anchor_assert`（价值最高、纯 stdlib、可参数化）；`cli.py` 2 行注册；`script list/info/run` 打通。**不动任何既有分析器。**
- **阶段 B**：补 A2/A3 + B1/B2；接 `gate`/CI 用 `dangerous_api_scan --fail-on-hit`。
- **阶段 C**：`script sweep` 接入普查器 + `doctor` 自检 + `snapshot/diff` 版本闭环 + 生成《使用手册》。

## 9. 风险与开放决策点（请评审拍板）

1. `sloc_summary` / `git_churn` 与工具既有 `overview LOC`、`hotspot/git_log_analyzer` **能力重叠**——是"并入既有分析器"还是"保留为独立轻量速查脚本"？（本稿默认后者，用 `overlap` 字段标注，倾向零改内核。）
2. `script run` 用 importlib 进程内调用还是 subprocess 隔离？（本稿默认 importlib，快且可共享地基；若某脚本可能破坏进程态再对该条走 subprocess。）
3. Tier C 归档是否只保留"清单+一句话"，还是连 `origin.source_sha256` 全量登记？（本稿默认登记哈希，便于日后判断源脚本是否已变。）
4. 注册表格式 `JSON` vs `YAML`：本稿用 JSON（与工具产物 `verorun_insight.json` 一致、无新依赖）；若团队更喜 YAML 可换。

---

## 附录 A：本轮已实际运行的验证证据

- 普查器 `inventory.py`、打分器 `score.py` 在 `C:\Users\Administrator\.qwenworkcn\workspace\mtxmgrfn2oh6uuhh`（**临时暂存区，非 VeroRun 系统**）**已实跑**：遍历 24 工作空间 → 249 去噪候选 → 33 纯标准库 / 37 有 `__main__` / 14 可参数化 / 21 高复用短名单（数字见 §2.2，可复跑 `stat.py` 复核）。
- 断言口径：去噪后候选 249、纯标准库 33、语法错误 2、短名单去重 21 —— 均为脚本直接统计输出，非估算。
- 精读验证：`scan_raw_conn_misuse / anchors / sloc / bloat_analysis / bloat_recent / attribution / copy_patchset / test_mcp_roundtrip / market_calendar / chatbot._stubs` 由子代理用 Read **逐行读取**后给出功能/输入/输出/依赖/通用性判词（见 §2.3、§4.2），未修改任何源文件（全程只读）。
- 架构断言：`config.py` 存在 `ROOT_LOCAL_SCRIPT_PREFIXES`；`cli.py:run()` 存在 `handlers` 分发字典；`__init__.py` 存在 `TOOL_VERSION/SCHEMA_VERSION` 及兼容注释；`snapshot.py` 存在 `build/save/load/list/diff/render/default_name` + `SNAPSHOT_VERSION` —— 均已 Read 核对，本方案的地基复用**属实**。

## 附录 B：交付物清单（实现轮产出，均落 `RepoLucent`）

`vr_insight/script_cmd.py`、`vr_insight/scriptlib/*`（6 脚本）、`vr_insight/registry.json`、`cli.py` 2 行注册补丁、《脚本库使用手册.md》、《脚本沉淀规范.md》。本轮仅提交本设计文档。

---

## 10. 阶段 A 落地与验收矩阵（本轮实际执行，非计划）

### 10.1 已写入 `RepoLucent` 的文件

| 文件 | 作用 | 性质 |
|---|---|---|
| `vr_insight/scriptlib/__init__.py` | 脚本库包 + `SCRIPTLIB_VERSION` | 新增 |
| `vr_insight/scriptlib/dangerous_api_scan.py` | 危险调用模式扫描器（规则数据化） | 新增（Tier A，实跑验证） |
| `vr_insight/scriptlib/anchor_assert.py` | 锚点断言器（claims 外置，只读） | 新增（Tier A，实跑验证） |
| `vr_insight/registry.json` | 脚本注册表（2 active + 4 planned + 3 archived，含真实 sha256 溯源） | 新增 |
| `vr_insight/script_cmd.py` | `script` 子命令分发（list/info/run/doctor） | 新增 |
| `vr_insight/cli.py` | **仅 3 处加法式改动**：import 1 行 + `script` 子解析器 + handlers 加 1 键 | 增量，既有分析器未动 |

> 边界确认：扫描各智能体工作空间全程**只读**；未向任何 VeroRun 产品目录写入。

### 10.2 §9 开放决策 —— 已锁定为推荐值

1. 重叠能力（sloc/churn vs 既有 overview/hotspot）：**并存为独立脚本**，用 `overlap` 字段标注口径差异，零改内核。
2. `script run` 调用方式：**importlib 进程内调用**（快、共享地基）；入口强制 `vr_insight.scriptlib.` 前缀白名单，越界拒绝（rc3）。
3. 归档登记粒度：**记录 `origin.source_sha256`**（archived 不入库运行，仅溯源）。
4. 注册表格式：**JSON**（与产物 `verorun_insight.json` 一致，无新依赖）。

### 10.3 验收矩阵（本轮 `insight.py` 实跑结果）

| 用例 | 命令 | 期望 | 实测 |
|---|---|---|---|
| 编目 | `script list` | 列 9 条、rc0 | ✅ 9 条，rc0 |
| 过滤 | `script list --category analysis` | 3 条 | ✅（REMAINDER 参数本地解析） |
| 过滤+机读 | `script list --status active --json` | active=2 | ✅ 计数 2 |
| 详情 | `script info dangerous_api_scan` | 完整元数据 | ✅ |
| 自检 | `script doctor` | 2 条 PASS，rc0 | ✅ 2/2 PASS |
| 扫描·正 | `script run dangerous_api_scan --root .` | 本项目 0 命中，rc0 | ✅ |
| 扫描·命中能力 | 造 `bad.py`(裸连 execute)+`good.py`(PgConnection 包装) | 抓 bad 行、放过 good、rc1 | ✅ 命中 `bad.py:3`，good 未报，rc1 |
| 断言·混合 | `script run anchor_assert --claims(5条含2故意错)` | 3 PASS/2 FAIL，rc1 | ✅ 命中真实行号、FAIL 判据正确 |
| 拒绝·planned | `script run sloc_summary` | rc2 + 提示阶段 B | ✅ |
| 拒绝·越界id | `script run not_in_registry` | rc2 白名单外拒绝 | ✅ |
| 独立运行 | `python -m vr_insight.scriptlib.dangerous_api_scan` | 可脱离子命令运行 | ✅ |
| 零改内核回归 | `insight.py --help` + `compileall vr_insight` | 既有子命令齐全、全包编译通过 | ✅ serve/snapshot/diff/query/ci-check 均在，编译通过 |

### 10.4 后续

- ~~B：落地 `sloc_summary`、`git_churn`、`offline_db_stub`、`subprocess_harness`~~ —— **已完成，见 §11**。
- C（待做）：`script sweep` 接入普查器（`script_sweep_prototypes/`）产出候选建议 + `script snapshot/diff` 复用 `snapshot.py` 做注册表基线对比 + 自动生成《脚本库使用手册.md》（数据源即 `registry.json`，文档与实现不分叉）。

---

## 11. 阶段 B 落地与验收矩阵（本轮实际执行）

### 11.1 新增文件与注册表状态变更

| 脚本 | 提炼自（真实只读精读） | 关键泛化改造 |
|---|---|---|
| `scriptlib/sloc_summary.py` | `sloc.py`（sha `c23a662b…`） | 去硬编码 ROOT→`--root`；复用 `DEFAULT_EXCLUDE_DIRS`；`overlap` 标注与 overview 口径差异 |
| `scriptlib/git_churn.py` | `bloat_analysis.py`+`bloat_recent.py`（sha `231738e0…`/`8121a447…`） | 两脚本合一；`--repo/--since/--group-by{dir,plugin,file}`；无 git/非仓库→rc2 |
| `scriptlib/offline_db_stub.py` | `chatbot/tests/_stubs.py`（sha `be0bf743…`） | 表无关化：`SimpleTable(key_cols)+add_table` 自动支持 CRUD；`install_stub(module,db)`；内置自检 |
| `scriptlib/subprocess_harness.py` | `test_mcp_roundtrip.py`（sha `1ebe8226…`） | 去 numpy/mcp_server 绑定；`JsonRpcProc+roundtrip(用例+期望谓词)`；内置自给 demo |

注册表：`sloc_summary / git_churn / offline_db_stub / subprocess_harness` 四条由 `planned → active`（version `0.0.0 → 1.0.0`，填 entry/inputs/outputs/deps/usage）；active 由 2 增至 **6**，`offline_db_stub` 的 `origin.sha256` 由 null 补为真实值。

### 11.2 阶段 B 验收（`insight.py` 实跑）

| 用例 | 命令 | 结果 |
|---|---|---|
| 自检扩面 | `script doctor` | ✅ 6/6 PASS，rc0 |
| 注册表扩面 | `script list --status active --json` | ✅ active=6 |
| 代码量 | `script run sloc_summary --root vr_insight` | ✅ Python 7057 行/37 文件，按目录榜单 |
| churn 归因 | 造一次性 git 仓（plugins/beta +300、alpha +100、docs +500） | ✅ 净增 400（docs 被排除），beta/alpha 精确归因，两种 group-by 均对，rc0 |
| 离线桩 | `script run offline_db_stub`（6 条断言） | ✅ SELECT/INSERT/UPDATE/commit/全表/rollback 6/6 PASS，rc0 |
| 往返协议 | `script run subprocess_harness`（demo 4 用例） | ✅ initialize/tools/未知方法/通知不挂起 4/4 PASS，rc0 |
| 零改内核回归 | `compileall vr_insight` | ✅ 全包编译通过 |

### 11.3 本轮捕获并修复的缺陷（真实回归，非事前设计）

1. **harness 默认动作误判**：`offline_db_stub/subprocess_harness` 的 `main` 曾用进程级 `sys.argv[1:]` 判定"是否跑自检"，经 `insight.py script run` 调用时该值恒为 `['script','run',...]`（非空）→ 走了"打印文档"分支而非自检。改为只依据传入的 `argv` 规整后判定。
2. **`git_churn` 兜底基准取错提交**：历史不足时间窗时用 `rev-list --max-count=1 HEAD` 取到的是最新 HEAD 自身，diff 自己=0。改为取根提交 `rev-list --max-parents=0 HEAD`，并对无提交仓库加 rc2 守卫。
3. **`offline_db_stub` 大小写解析分叉**：`FakeDB.execute` 把大小写保留的 `norm` 传给处理器，而处理器已按小写解析 `where/set` → 大写 SQL 下 UPDATE 静默不生效、SELECT 不过滤（首轮自检 1 FAIL/1 崩溃）。统一在归一化 `low` 上解析并回传。

> 三处缺陷均由运行自检暴露后修复并复测通过，符合"示例代码须实际执行验证"的交付标准。

---

## 12. 阶段 C + UI 接入落地与验收（本轮实际执行）

### 12.1 UI 接入决策（经用户确认）

- **只读发现**：把 `script list/info` 的元数据接进 `serve`（浏览器面板 + Agent HTTP API），零执行、零风险，契合 serve「不任意执行」边界。
- **受控 run**：应你要求"现在一起做"——但**不做通用 dispatcher**，而是三重门控复刻 `git/push` 的审慎范式：① 仅 `status==active 且 kind==tool` 的白名单脚本可经 HTTP 运行（harness/planned/archived 一律拒绝）；② `confirm==false` 只返回 dry-run 命令预览、不执行；③ serve 本就 `127.0.0.1`-only。CLI 的 `script run` 仍是完整能力（含 harness 自检）。

### 12.2 新增/改动文件

| 文件 | 变更 | 性质 |
|---|---|---|
| `vr_insight/scriptlib/script_sweep.py` | 一等化普查器（发现候选、四维打分、标注是否已入库；对被扫目录只读） | 新增 |
| `vr_insight/registry.json` | 新增 `script_sweep`（active）；脚本数 9→10，active 6→7 | 增量 |
| `vr_insight/script_cmd.py` | 加动作 `sweep/snapshot/diff/manual` + 抽出 `_resolve_entry_and_run` + 供 serve 的 `run_script_api`（三重门控）；`_render_manual` 从 registry 单一事实源渲染手册 | 增量 |
| `vr_insight/cli.py` | `script` 的 action 取值域扩到 8 个（仍加法式） | 增量 |
| `vr_insight/server.py` | GET `/api/scripts`、GET `/api/scripts/<id>`、POST `/api/scripts/run`；顶部 API 文档同步；控制台新增「🧩 脚本资产库」面板 + 交互 JS | 增量，未碰既有 Git 路由 |
| `脚本库使用手册.md` | 由 `script manual` 自动生成（任务⑦交付物，数据源=registry，不分叉） | 生成物 |

版本管理（§6 承诺兑现）：`script snapshot <名> [--out]` 把当前注册表指纹存 `out/history/scripts/<名>.json`（复用 `snapshot.py` 的 history 目录/`_safe_name`/`_dumps` 约定，前缀 `scripts_` 与仓库基线隔离）；`script diff <基线>` 输出新增/删除/字段变更。

### 12.3 阶段 C 验收（实跑）

| 用例 | 命令/接口 | 结果 |
|---|---|---|
| 自检扩面 | `script doctor` | ✅ 7/7 PASS |
| 发现初筛 | `script sweep tests --min-score 5` | ✅ 16 候选、榜单打分、★入库标注 |
| 基线 | `script snapshot base-v1 --out out` | ✅ 落 `history/scripts/base-v1.json`（10 条） |
| 无变更 | `script diff base-v1` | ✅ “无变更” |
| 变更检出 | 造漂移基线后 `script diff` | ✅ 精确报出 新增1/删除1/`sloc_summary version 0.9.0→1.0.0` |
| 手册生成 | `script manual --out 脚本库使用手册.md` | ✅ 单一事实源渲染，含依赖/重叠/来源 |
| 全包编译 | `compileall vr_insight` | ✅ 通过；`--help` 既有子命令齐全 |
| GET 列表 | `GET /api/scripts` | ✅ 200，count=10 |
| GET 详情 | `GET /api/scripts/dangerous_api_scan` | ✅ 200 完整元数据 |
| GET 未知 | `GET /api/scripts/nope` | ✅ 404 |
| run dry-run | `POST /api/scripts/run {sloc_summary,confirm:false}` | ✅ 返回命令预览，未执行 |
| run 执行 | `POST {dangerous_api_scan,confirm:true}` | ✅ ok，exit_code=0 |
| run 拒 harness | `POST {offline_db_stub,confirm:true}` | ✅ `not_runnable_via_api` + 提示 |
| run 未确认 | `POST {git_churn,confirm:false}` | ✅ dry-run 预览 |
| UI 面板 | `GET /` 页面 | ✅ 含「脚本资产库」区块 + `loadScripts`/运行按钮 |

> 边界确认：serve 测试以 VeroRun 工作树作 `--repo` 仅做**只读分析**，报告写临时 `out_serve_test/`；全程未写入任何产品代码；测试服务按端口精确关闭、无误伤。
