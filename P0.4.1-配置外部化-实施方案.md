# P0 §4.1 配置外部化 · 实施方案（逐文件报批）

> 状态：**方案阶段，未改任何核心代码**。待确认后按本文件逐文件落地。
> 依据：设计文档 §4.1（横切地基，解锁 LLM 与 MCP）。

## 1. 背景与目标

设计文档 §4.1 原文：

> 现状 `config.py` 全硬编码、`class RepoConfig`（`A11`）里 `max_plugins_in_ai_context` 这类字段（`A12`）就是新增 `models`/`mcp` 配置的同构落点。方案：新增 `settings.json`（可选）+ 环境变量覆盖，扩 `RepoConfig` 或新建 `ToolConfig` dataclass；沿用 `__init__.py` 的 `SCHEMA_VERSION` 兼容约定（MINOR 只增可选字段、消费方忽略未知）。密钥只 `os.environ.get`，绝不进 `settings.json` 明文入库。

**验收门（文档 §5）**：`settings` 能注入而源码不动。

**解锁对象**：P2 LLM（`models[]` / 密钥）、MCP 配置的外部化；`max_*` 上限等不再需要改 `config.py`。

## 2. 设计原则

1. **向后兼容（最高优先）**：无 `settings.json` 时，行为与现版本完全一致（纯增量）。
2. **优先级链**：`CLI args` > `settings.json` > 内置默认。
3. **密钥隔离**：`api_key` / `base_url` 等敏感项**只**从环境变量 `VR_INSIGHT_*` 读取；`settings.json` 只承载非敏感配置。
4. **兼容契约**：`settings` 字段均为可选、MINOR 级；消费方忽略未知字段（沿用 `SCHEMA_VERSION` 约定）。
5. **零第三方依赖**：仅用标准库 `json` / `os` / `pathlib`。
6. **不侵入 VeroRun 系统代码**：仅改 `RepoLucent` 内部。

## 3. 文件改动清单

| 文件 | 动作 | 说明 |
|---|---|---|
| `vr_insight/settings.py` | **新增** | 配置加载器：`settings.json` 搜索链 + 环境变量 + 密钥隔离 API |
| `vr_insight/config.py` | **修改** | 新增 `ToolConfig` dataclass + `from_settings()`（任务级 `RepoConfig` 之下的工具级配置） |
| `vr_insight/settings.default.json` | **新增** | 包内默认配置（随包发布，兜底） |
| `vr_insight/cli.py` | **修改** | `_setup` 注入 `ToolConfig` 默认值（CLI args 仍最高优先） |
| `vr_insight/server.py` | **修改** | `/mcp` 分支加 `mcp_enabled` 守卫 |
| `SETTINGS.md` | **新增** | 配置 schema 与环境变量命名约定文档 |
| `verify_settings.py` | **新增** | 验收脚本：无配置等价旧行为 + 有配置注入生效 |

> 依赖方向：`config.py` → `settings.py`（单向 import）；`cli.py` / `server.py` → 两者。无循环依赖。

## 4. 逐文件改动细节 + 示例代码

### 4.1 `vr_insight/settings.py`（新增）

```python
# -*- coding: utf-8 -*-
"""配置外部化核心：settings.json（可选）+ 环境变量覆盖。

- settings.json 只承载非敏感配置（max_*、models 元数据、mcp 开关等）。
- 密钥（api_key / base_url 等）一律只从环境变量读，绝不进 settings.json 明文。
- 沿用 SCHEMA_VERSION 兼容约定：MINOR 只增可选字段，消费方忽略未知。
"""
from __future__ import annotations

import json
import os
from pathlib import Path

_HERE = Path(__file__).resolve().parent
_TOOL_ROOT = _HERE.parent
_DEFAULT_NAME = "settings.json"


def _search_paths() -> list[Path]:
    """settings.json 搜索链（第一个存在者胜出）：
    1) 环境变量 VR_INSIGHT_SETTINGS  2) <tool>/settings.json
    3) ~/.repolucent/settings.json  4) 包内 settings.default.json（兜底）
    """
    paths: list[Path] = []
    if env := os.environ.get("VR_INSIGHT_SETTINGS"):
        paths.append(Path(env))
    paths.append(_TOOL_ROOT / _DEFAULT_NAME)
    paths.append(Path.home() / ".repolucent" / _DEFAULT_NAME)
    paths.append(_HERE / "settings.default.json")
    return paths


def load_settings() -> dict:
    """返回合并后的配置 dict；任何文件缺失/损坏都不致命（降级为空 dict）。"""
    merged: dict = {}
    for p in _search_paths():
        if p.is_file():
            try:
                merged.update(json.loads(p.read_text(encoding="utf-8")))
            except Exception:  # noqa: BLE001  —— 损坏配置静默降级，不影响工具运行
                pass
    return merged


def get_secret(key: str, default: str | None = None) -> str | None:
    """密钥只从环境变量读取（如 VR_INSIGHT_OPENAI_KEY）。绝不经 settings.json。"""
    return os.environ.get(key, default)


def get_setting(key: str, default=None, *, env_var: str | None = None):
    """非敏感项：settings.json 优先，其次环境变量，其次 default。"""
    s = load_settings()
    if key in s and s[key] is not None:
        return s[key]
    if env_var and (v := os.environ.get(env_var)) is not None:
        return v
    return default
```

### 4.2 `vr_insight/config.py`（修改）

在 `RepoConfig` 之后新增 `ToolConfig`（工具/部署级，区别于任务级 `RepoConfig`）：

```python
from .settings import load_settings  # 顶部新增 import（单向，无循环）


@dataclass
class ToolConfig:
    """工具级配置（跨任务共享），来自 settings.json / 环境变量。"""
    mcp_enabled: bool = True
    llm_enabled: bool = False
    default_model: str | None = None
    models: list[dict] = field(default_factory=list)   # P2 使用：[{name, provider, model, key_env, base_url_env}]
    max_plugins_in_ai_context: int = 60
    max_tree_depth: int = 2

    @classmethod
    def from_settings(cls) -> "ToolConfig":
        s = load_settings()
        return cls(
            mcp_enabled=bool(s.get("mcp_enabled", True)),
            llm_enabled=bool(s.get("llm_enabled", False) or s.get("models")),
            default_model=s.get("default_model"),
            models=list(s.get("models", [])),
            max_plugins_in_ai_context=int(s.get("max_plugins_in_ai_context", 60)),
            max_tree_depth=int(s.get("max_tree_depth", 2)),
        )
```

### 4.3 `vr_insight/settings.default.json`（新增，随包发布）

```json
{
  "mcp_enabled": true,
  "max_plugins_in_ai_context": 60,
  "max_tree_depth": 2
}
```

### 4.4 `vr_insight/cli.py`（修改）

`_setup` 注入 `ToolConfig` 默认值；**CLI args 仍最高优先**（向后兼容）：

```python
def _setup(args, with_target: bool = False) -> RepoConfig:
    cfg = RepoConfig.autodetect_repo(getattr(args, "repo", None), str(script_file))
    tc = ToolConfig.from_settings()                       # ← 新增
    # 原硬编码默认值 2 / 60 改为优先取自 ToolConfig（可被 settings.json 覆盖）
    cfg.max_tree_depth = getattr(args, "tree_depth", None) or tc.max_tree_depth
    cfg.max_plugins_in_ai_context = tc.max_plugins_in_ai_context
    cfg.extra_repos = list(getattr(args, "extra_repos", None) or [])
    if with_target:
        if getattr(args, "module", None):
            cfg.target, cfg.target_type = args.module, "module"
        elif getattr(args, "plugin", None):
            cfg.target, cfg.target_type = args.plugin, "plugin"
    if getattr(args, "out", None):
        cfg.out_dir = Path(args.out).resolve()
        cfg.out_dir.mkdir(parents=True, exist_ok=True)
    return cfg
```

### 4.5 `vr_insight/server.py`（修改）

`_route_post` 的 `/mcp` 分支加 `mcp_enabled` 守卫（默认 True，向后兼容）：

```python
if u.path == "/mcp":
    if not ToolConfig.from_settings().mcp_enabled:
        self._send(*_err(404, "NotEnabled", "MCP 已在 settings 中禁用"))
        return
    # ... 既有 handle_jsonrpc 逻辑不变
```

（需 `from .config import ToolConfig, RepoConfig` 已存在，第 51 行已 import `RepoConfig`，补 `ToolConfig` 即可。）

### 4.6 `SETTINGS.md`（新增）

说明：搜索链、可配置字段表、`VR_INSIGHT_*` 环境变量命名约定、密钥禁用规则、`SCHEMA_VERSION` 兼容说明。

### 4.7 `verify_settings.py`（新增，验收脚本）

| 用例 | 操作 | 期望 |
|---|---|---|
| 无 `settings.json` | 跑 `insight.py summary` | `AI_CONTEXT` 插件表 = 60 行（等价旧行为） |
| 有 `settings.json`（`max_plugins_in_ai_context=10`） | 跑 `insight.py summary` | `AI_CONTEXT` 插件表 ≤ 10 行（**源码未改**，仅配置生效） |
| 密钥隔离 | `get_secret("VR_INSIGHT_TEST_KEY")` 从 `env` 读；`settings.json` 内写 `test_key` 不被读取 | 仅环境变量命中 |

## 5. settings.json schema（示例）

```json
{
  "mcp_enabled": true,
  "max_plugins_in_ai_context": 60,
  "max_tree_depth": 2,
  "models": [
    {
      "name": "qwen-plus",
      "provider": "openai_compat",
      "model": "qwen-plus",
      "key_env": "VR_INSIGHT_DASHSCOPE_KEY",
      "base_url_env": "VR_INSIGHT_DASHSCOPE_BASE_URL"
    }
  ],
  "default_model": "qwen-plus"
}
```

> ⚠️ **密钥禁写**：`settings.json` 中不得出现 `api_key` / `base_url` 明文；这类字段只允许通过 `key_env` / `base_url_env` 指向的环境变量解析。

## 6. 与 P2 的衔接

- `get_secret()` 是 P2 `llm/providers/openai_compat.py` 的密钥读取通道：`get_secret(model["key_env"])`。
- `ToolConfig.models[]` 是 P2 `llm/runner.py` 的多模型配置来源。
- 本阶段只铺设读取通道与默认值注入，不新增 `llm/` 业务代码（P2 负责）。

## 7. 风险与回滚

- **风险 A（配置损坏）**：`load_settings` 捕获异常静默降级为空 dict → 退回全部内置默认，工具不崩。
- **风险 B（未知字段）**：消费方只取已知 key，忽略未知 → 向前兼容。
- **回滚**：删除 `settings.py` / `settings.default.json` / `ToolConfig` 引用即回到全硬编码；纯增量，不动既有 CLI 行为与 `/mcp`、stdio 已验证路径。
- **影响面**：仅 `cli._setup` 与 `server._route_post` 两处消费点新增 `ToolConfig` 读取；已有 6/6 MCP 验证与 4/4 HTTP 验证路径不变（默认 `mcp_enabled=True`）。

## 8. 落地顺序建议

1. `settings.py` + `settings.default.json`（地基）
2. `config.py` `ToolConfig` + `from_settings()`
3. `cli.py` `_setup` 注入
4. `server.py` `mcp_enabled` 守卫
5. `SETTINGS.md` + `verify_settings.py`
6. 跑 `verify_settings.py` 三用例，确认验收门通过
