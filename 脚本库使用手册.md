# 脚本资产库 · 使用手册

> 本手册由 `insight.py script manual` 从 `vr_insight/registry.json` 自动生成，请勿手改；更新时间 2026-09-12 17:18。

脚本元 schema：`1.0` ｜ 注册表版本 `1.0`。共 12 条（active/planned/archived 见下）。

## 快速上手

```bash
python insight.py script list
python insight.py script info <id>
python insight.py script run <id> [脚本参数...]
python insight.py script doctor
```

## `anchor_assert` — 改动锚点断言器

- 状态/类型：`active` / `tool` ｜ 类别 `verification` ｜ 版本 `1.0.0`
- 功能：把报告/补丁示例引用的改动锚点外置成 claims.json，逐条对真实仓库断言『文件存在且期望原文子串仍在（可选行号）』。任一条 FAIL 即说明文档会打歪。纯只读。
- 输入：`--against`(path)、`--claims`(file)、`--json`(flag,可选)、`--stop-on-first`(flag,可选)
- 输出：逐条 [PASS/FAIL] id file — reason ｜ 退出码 0 全PASS / 1 有FAIL / 2 参数错
- 依赖：仅标准库
- 用法：`insight.py script run anchor_assert --against <repo> --claims claims.json`
- 注意：把『读真实文件→断言锚点→打补丁』方法论工具化；claims 由报告生成器产出，勿硬编码进脚本。
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mtwrnvpl2scozvdp/verify/anchors.py` (sha `1587e5976c18`，提炼于 2026-09-12)

## `dangerous_api_scan` — 危险 API / 调用模式静态扫描

- 状态/类型：`active` / `tool` ｜ 类别 `analysis` ｜ 版本 `1.0.0`
- 功能：扫描仓库中『取原始资源的变量被危险方法直接调用且未经包装』的模式；规则数据化、可 --rules 注入适配任意框架。退出码 0/1 可作 CI 红线。
- 输入：`--root`(path,可选,默认 .)、`--rules`(file,可选)、`--json`(flag,可选)、`--fail-on-hit`(flag,可选)
- 输出：命中清单 [rule] file:line: snippet ｜ 退出码 0 无命中 / 1 有命中 / 2 参数错
- 依赖：仅标准库
- 复用平台地基：vr_insight.config.DEFAULT_EXCLUDE_DIRS
- ⚠ 能力重叠：与 gate.py 既有门禁互补（gate 管体量/结构，本脚本管跨行调用链误用）
- 用法：`insight.py script run dangerous_api_scan --root <repo> --fail-on-hit`
- 注意：内置规则针对数据库裸连接；适配其它框架请提供 --rules JSON。只读，不改源文件。
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mtvjkbolno04xf4s/outputs/verification/scan_raw_conn_misuse.py` (sha `5c0e367defc2`，提炼于 2026-09-12)

## `git_churn` — Git 增量归因

- 状态/类型：`active` / `tool` ｜ 类别 `git-workflow` ｜ 版本 `1.0.0`
- 功能：git numstat 按时间窗/目录/插件/文件净增归因，定位代码膨胀源（合并 bloat_analysis + bloat_recent）。
- 输入：`--repo`(path,可选,默认 .)、`--since`(str,可选,默认 30.days)、`--group-by`(enum,可选,默认 dir)、`--top`(int,可选,默认 15)、`--json`(flag,可选)
- 输出：窗口净增合计 + 按组 Top + 净增 Top 文件 ｜ 退出码 0 成功 / 2 非git仓或无git
- 依赖：需 git(可执行)；需 git 仓库
- 复用平台地基：vr_insight.config.DEFAULT_EXCLUDE_DIRS
- ⚠ 能力重叠：与 hotspot_analyzer / git_log_analyzer 部分重叠；本脚本聚焦『净增行数归因』。
- 用法：`insight.py script run git_churn --repo <r> --since 90.days --group-by plugin`
- 注意：历史不足时间窗时取最早点近似并提示；只读，不改仓库。
- 来源：`F:/共享文件夹/verorun-dev-insight/tests/bloat_analysis.py + bloat_recent.py` (sha `231738e05cad`，提炼于 2026-09-12)

## `offline_db_stub` — 离线数据库替身桩

- 状态/类型：`active` / `harness` ｜ 类别 `testing` ｜ 版本 `1.0.0`
- 功能：表无关的内存 SQL 语义替身 + install_stub 边界 monkeypatch；不连真库跑离线单测。CLI 仅跑内置自检。
- 输入：`--self-test`(flag,可选)
- 输出：自检逐条 PASS/FAIL ｜ 退出码 0 自检通过 / 1 失败
- 依赖：仅标准库
- 用法：`from vr_insight.scriptlib.offline_db_stub import FakeDB, SimpleTable, install_stub  # 被测代码以 `with _db() as conn` 为契约`
- 注意：只实现被测代码用到的 SQL 子集（按归一化前缀分派）；未知语句抛 AssertionError 以便暴露缺口。原桩硬绑 chatbot，本桩泛化。
- 作为库导入：`FakeDB / SimpleTable / install_stub / FakeCursor`
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mtwrnvpl2scozvdp/outputs/AI-Advisor-patch-set/plugins/chatbot/tests/_stubs.py` (sha `be0bf743c690`，提炼于 2026-09-12)

## `script_sweep` — 脚本普查器（发现候选）

- 状态/类型：`active` / `tool` ｜ 类别 `analysis` ｜ 版本 `1.0.0`
- 功能：扫描目录内 .py，去噪后按四维打分给出『值得提炼入库』的候选并标注是否已登记；脚本资产库的发现入口，dogfooding 自身。
- 输入：`root`(path,可选,默认 .)、`--min-score`(int,可选,默认 6)、`--limit`(int,可选,默认 40)、`--json`(flag,可选)
- 输出：候选榜单（★=已入库） ｜ 退出码 0 成功 / 2 参数错
- 依赖：仅标准库
- 用法：`insight.py script sweep <目录> [--min-score 6] [--json]`
- 注意：分数仅客观初筛，是否入库由人工定 Tier；对被扫目录严格只读。
- 来源：`verorun-dev-insight/script_sweep_prototypes/{inventory.py,score.py}` (sha `—`，提炼于 2026-09-12)

## `sloc_summary` — 轻量代码量速查

- 状态/类型：`active` / `tool` ｜ 类别 `analysis` ｜ 版本 `1.0.0`
- 功能：cloc 风格 code/comment/blank 按语言+顶层目录统计，面向任意目录速查。
- 输入：`--root`(path,可选,默认 .)、`--top`(int,可选,默认 15)、`--json`(flag,可选)、`--out`(file,可选)
- 输出：按语言/按顶层目录双维榜单 ｜ 退出码 0 成功 / 2 参数错
- 依赖：仅标准库
- 复用平台地基：vr_insight.config.DEFAULT_EXCLUDE_DIRS
- ⚠ 能力重叠：与 vr_insight 既有 overview LOC 口径不同（那是 VeroRun 全仓+本地脚本排除）；本脚本为通用速查，勿与主命令混用。
- 用法：`insight.py script run sloc_summary --root <dir> [--json]`
- 注意：语言注释语法表内置；未知扩展名跳过；只读。
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mthwrvt2p917d3hz/sloc.py` (sha `c23a662bbf28`，提炼于 2026-09-12)

## `ssh_readonly_probe` — VeroRun 服务器 只读 SSH 探针

- 状态/类型：`active` / `tool` ｜ 类别 `testing` ｜ 版本 `1.0.0`
- 功能：经 SSH 采集部署版本/git、商店相关 .env（脱敏）、systemd 服务态、负载与服务器侧 store_catalog 可达性；内置破坏性命令拒绝名单，仅执行只读命令，可 --cmd 追加单条只读命令。
- 输入：`--host`(string)、`--port`(int,可选,默认 22)、`--repo`(path,可选,默认 /home/guxiao/verorun)、`--cmd`(string,可选)、`--timeout`(int,可选,默认 60)、`--json`(flag,可选)
- 输出：逐条命令 $ 回显 + 超时/退出码 + 结论 ｜ 退出码 0 正常 / 1 检出超时或非零退出(降级) / 2 环境错(缺 paramiko/凭证/连接失败) / 3 内部异常
- 依赖：需 paramiko；含网络
- ⚠ 能力重叠：与 store_probe 互补：一个走 HTTP 外部面，一个走服务器内部面；均只读。
- 用法：`insight.py script run ssh_readonly_probe --host <ip> --repo <path>  （先 export VR_SSH_USER VR_SSH_PASS）`
- 注意：paramiko 惰性导入，未装时 script doctor 仍通过、仅 run 返回 2。凭证仅环境变量，拒绝 rm/mkfs/dd/>/systemctl stop|restart/git push 等破坏性 token。
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mty3em275wj2u0cs/ssh_probe.py` (sha `93663c11148b`，提炼于 2026-09-12)

## `store_probe` — VeroRun 应用商店 只读/越界门控 真实环境探针

- 状态/类型：`active` / `tool` ｜ 类别 `testing` ｜ 版本 `1.0.0`
- 功能：对给定商店后端执行一组【只读 + 鉴权越界门控】用例（就绪/公开浏览最小权限/登录浏览/详情裁剪/兼容判定/无token写端点403/404/输入加固），逐条 PASS/FAIL 并支持 --json。凭证只从环境变量取，不做任何真实变更。
- 输入：`--base-url`(url,可选,默认 https://agent.easykai.cn)、`--prefix`(string,可选,默认 /admin/plugins)、`--token-env`(string,可选)、`--timeout`(int,可选,默认 25)、`--json`(flag,可选)
- 输出：逐条 [PASS/FAIL/SKIP] id desc | detail + 合计 ｜ 退出码 0 全通过 / 1 有失败 / 2 参数或网络不可达
- 依赖：仅标准库；含网络
- ⚠ 能力重叠：与主命令无重叠；被测对象为运行期商店后端而非静态仓库。
- 用法：`insight.py script run store_probe --base-url https://<host> [--json]`
- 注意：仅 GET 与『无凭证写端点探测(预期403, 不落库)』；凭证走环境变量 VR_STORE_TOKEN 或 VR_STORE_USER/VR_STORE_PASS，绝不入 argv/文件。生产慎用 --base-url 指向正式站。
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mty3em275wj2u0cs/api_probe.py` (sha `02433bbcfc81`，提炼于 2026-09-12)

## `subprocess_harness` — 子进程往返协议验证模板

- 状态/类型：`active` / `harness` ｜ 类别 `testing` ｜ 版本 `1.0.0`
- 功能：对 stdio JSON-RPC 子进程按『用例(方法+期望谓词)』跑往返；内置自给自足 demo 验证 initialize/tools/未知方法/通知不挂起。
- 输入：`--demo`(flag,可选)
- 输出：逐用例 PASS/FAIL ｜ 退出码 0 全通过 / 1 有失败 / 2 参数错
- 依赖：仅标准库
- 用法：`from vr_insight.scriptlib.subprocess_harness import JsonRpcProc, roundtrip  # 换成本项目的 server 启动命令与用例即可`
- 注意：Windows 下 PYTHONPATH 用 os.pathsep 拼接；read 按 id 匹配、跳过通知，防挂起。原脚本硬绑 mcp_server+numpy，本模板参数化。
- 作为库导入：`JsonRpcProc / roundtrip / has_result / has_error / expect_field`
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mtjmjntr5od7ygsn/verify_sa/test_mcp_roundtrip.py` (sha `1ebe8226f7a5`，提炼于 2026-09-12)

## `attribution` — LOC 来源裁表（一次性）

- 状态/类型：`archived` / `tool` ｜ 类别 `analysis` ｜ 版本 `0.0.0`
- 功能：读工具产出的 verorun_insight.json 按语言/目录解释行数来源，绑本次审计 JSON。
- 依赖：标准库为主
- 来源：`F:/共享文件夹/verorun-dev-insight/tests/attribution.py` (sha `7068dc14465c`，提炼于 2026-09-12)

## `copy_patchset` — 补丁集搬运+清单

- 状态/类型：`archived` / `tool` ｜ 类别 `ops` ｜ 版本 `0.0.0`
- 功能：把 worktree 指定文件复制到交付目录并生成 MANIFEST(行数+sha256)。绑定特定文件清单，一次性。
- 依赖：标准库为主
- 注意：archived：不入库运行；如需请 script sweep 重新提炼。搬运逻辑可借鉴。
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mtwrnvpl2scozvdp/verify/copy_patchset.py` (sha `d1948adb5646`，提炼于 2026-09-12)

## `market_calendar` — A股交易日历（跨域）

- 状态/类型：`archived` / `tool` ｜ 类别 `util` ｜ 版本 `0.0.0`
- 功能：离线 CSV 优先+周末兜底的交易日历库模块。通用但属行情业务域，与代码分析工具跑题，不入库。
- 依赖：标准库为主
- 来源：`C:/Users/Administrator/.qwenworkcn/workspace/mtl3w1swg4pzphs3/verify_impl/scratch/calendar_mod/market_calendar.py` (sha `c0208e7958ce`，提炼于 2026-09-12)

---
归档/计划态脚本不入库运行；`archived` 仅溯源，`planned` 待后续阶段落地。新脚本沉淀流程见设计文档《脚本沉淀规范》与 §6 版本管理。
