# AGENTS.md

<!-- repolucent:begin auto（本块由 RepoLucent 生成，勿手改） -->
## 项目速览

| metric | value |
|---|---:|
| repository | `fixture_repo` |
| core_modules | 3 |
| plugins | 2 |
| routes | 2 |
| files | 13 |
| lines_code | 53 |
| schema_version | 1.4 |

> 本文件由 RepoLucent 自动生成的标记块维护；块外内容为仓库自有约定，不随工具运行变化。

## 组件契约

`BasePlugin` (`plugin_manager/base.py`)

| method | required |
|---|---|
| `setup(self)` | yes |
| `activate(self)` | yes |
| `deactivate(self)` | no |
| `t(self, text, locale=None)` | no |

发现规则（来自框架源码 docstring 原文）：

- Plugin Manager - PluginDiscovery.
- Rules:
- 1. Must be plugins/<name>/ subdir
- 2. Must contain __init__.py
- 3. Must contain plugin.json (valid JSON)
- 4. Ignore dirs starting with _ or .

## 清单必填字段与枚举

- file: `docs/plugin-manifest.schema.json`
- required: `identifier, name, version, description, author, min_app_version, agent_role, capabilities`
- enum `agent_role`: athena / content / business / builder
- enum `category`: system / shop / tools

## 既定惯例（实测统计）

- 路由 url_prefix 分布 Top 1：`'/admin/demo'`×2（合计 2 条路由）
- 插件最常导入的核心设施：`plugin_manager`×1

## 架构红线

- 核心模块不应直接 import 业务插件（plugin_manager 动态加载除外）；
- checked_files: 5；observed_violations: 0

## 命令速查

```bash
# 全量分析并落盘产物（json / md / html / ai 上下文 / 符号索引 / AGENTS.md）
repolucent --repo <repo>

# 只打印关键指标（stdout key=value，不落盘）
repolucent --repo <repo> --summary-only

# 架构门禁：命中即退出码 1（CI 可编程依赖）
repolucent --repo <repo> --fail-on manifest-invalid,boundary-violation,plugin-cycle

# 按需上下文切片（Agent 专用，不落盘）
repolucent --repo <repo> context --for plugin:<identifier> --depth brief

# 结构化查询与符号定位
repolucent --repo <repo> query --select identifier,version --where agent_role=<role>
repolucent --repo <repo> search <symbol>
```

- 可用门禁项（`--fail-on`）：`manifest-invalid`, `boundary-violation`, `plugin-cycle`, `route-unprefixed`, `file-too-large`, `findings`, `all`

## 文档索引

- `AGENTS.md`：AGENTS.md
- `docs/plugin-manifest.schema.json`：plugin-manifest.schema.json

---
*block generated by RepoLucent v2.0.0; schema 1.4*
<!-- repolucent:end auto -->
