# VeroRun Dev Insight 扩展方案

**——从"报告生成器"升级为"架构守门员 + Agent 事实源"**

| 项目 | 内容 |
|---|---|
| 目标版本 | v1.2.0 → v2.0.0 |
| 文档状态 | 阶段 A–E 已实施并验收通过；阶段 F 待实施 |
| 设计前提 | 工具本体**永不接入 LLM**；保持纯标准库、纯确定性；AI 消费由外部桌面 Agent 完成 |
| 影响范围 | 新增 4 个模块、修改 6 个模块，全部向后兼容现有用法 |

---

## 0. 设计原则（本次扩展的硬约束）

1. **确定性优先**：相同输入必须产生逐字节相同的输出（在 `--deterministic` 下）。任何时间戳、耗时、绝对路径都不得进入可 diff 产物。
2. **零第三方依赖**：只使用 Python 标准库（`ast` / `json` / `argparse` / `pathlib` / `dataclasses` / `hashlib` / `http.server`）。
3. **向后兼容**：`python insight.py --repo X` 这条最常用命令的行为与输出路径不得改变。新增能力一律走子命令或可选 flag。
4. **事实与判断分离**：工具只产出**事实**（file、line、rule_id、value），不产出**建议**（"你应该重构"）。判断交给 Agent 或人。
5. **机器友好优先于人类友好**：产物必须能被程序稳定解析，字段名变更即视为破坏性变更。

---

## 1. 能力总览

| 编号 | 能力 | 类型 | 阶段 | 解决的问题 |
|---|---|---|---|---|
| A | 确定性输出 + schema_version | 改造 | A | 产物不可 diff，Agent 无法判断字段兼容性 |
| B | `--summary-only` 摘要输出 | 新增 | A | Agent 摸现状要读 730KB JSON |
| C | AST 缓存与增量分析 | 新增 | B | 22 秒太慢，仓库翻倍即分钟级 |
| D | `--fail-on` 门禁退出码 | 新增 | C | CI 无法据此失败 |
| E | `snapshot` / `diff` 子命令 | 新增 | D | 代码膨胀归因靠人肉 `git log` |
| F | `context --for` / `query` | 新增 | E | AI_CONTEXT.md 是一刀切固定切片 |
| G | `rules/` 规则引擎 + findings | 新增 | F | 规范/安全/架构红线只能靠人守 |
| H | 结构化错误输出 | 改造 | A | 失败时只有一行 stderr，Agent 无法判断原因 |

阶段 A→F 为实施顺序，每阶段独立可交付、可回退。

---

## 2. 阶段 A：确定性输出 + schema_version + 摘要（最小改动，收益最大）

### 2.1 问题分析

当前 `cli.py:130-139` 构造的 `meta` 段：

```python
"meta": {
    "tool": "verorun-dev-insight",
    "tool_version": TOOL_VERSION,
    "generated_at": datetime.now().strftime("%Y-%m-%d %H:%M:%S"),  # ← 每次不同
    "repo_root": str(cfg.repo_root),                                # ← 绝对路径，跨机器不同
    "tree_depth": cfg.max_tree_depth,
    "tree": render_tree(cfg),                                       # ← 含绝对路径首行
    "duration_ms": round((time.perf_counter() - t0) * 1000),        # ← 每次不同
}
```

三次运行产生三份 diff 全量噪音的 JSON，无法做基线对比。

### 2.2 改造方案

**① 新增 `--deterministic` 开关**

```python
ap.add_argument("--deterministic", action="store_true",
                help="确定性输出：剥离时间戳/耗时/绝对路径，使产物可 diff")
```

`cli.py` 中构造 meta 时：

```python
def _build_meta(cfg, t0, tree, deterministic: bool) -> dict:
    meta = {
        "schema_version": SCHEMA_VERSION,          # 新增，见 2.3
        "tool": "verorun-dev-insight",
        "tool_version": TOOL_VERSION,
        "generated_at": None if deterministic
                        else datetime.now().strftime("%Y-%m-%d %H:%M:%S"),
        "repo_root": cfg.repo_root.name if deterministic else str(cfg.repo_root),
        "tree_depth": cfg.max_tree_depth,
        "tree": tree,
        "duration_ms": None if deterministic
                       else round((time.perf_counter() - t0) * 1000),
    }
    return meta
```

`render_tree()` 首行 `lines = [root.name + "/"]` 已经是相对名，无需改动。

**② 产物排序已稳定**：`scan_overview` 用 `Counter.most_common`（按计数降序，同计数按插入序）、`plugins["items"]` 已 `sorted(key=identifier)`、`by_top_dir` 已排序。经核查排序口径确定性达标，无需额外处理。

### 2.3 schema_version 契约

在 `vr_insight/__init__.py` 中新增：

```python
#: 产物 schema 版本。MAJOR=破坏性字段变更，MINOR=新增可选字段。
SCHEMA_VERSION = "1.0"
```

演进规则（写入 `vr_insight/__init__.py` 注释，供 Agent 与开发者共同遵守）：

- **MAJOR**：删除字段、重命名字段、改变字段类型或语义 → 必须同步更新 `report_*` 三个渲染器。
- **MINOR**：新增可选字段 → Agent 未识别的字段应忽略而非报错。

### 2.4 `--summary-only` 摘要输出

Agent 最高频的诉求是"先摸清现状"，不需要 730KB。新增开关：

```python
ap.add_argument("--summary-only", action="store_true",
                help="只在 stdout 打印关键指标摘要，不落盘任何文件")
```

输出格式（固定 11 行 `key=value`，便于 Agent 解析；不含任何提示行）：

```
repo=verorun-code
schema_version=1.0
core_modules=12
plugins=40
plugins_manifest_invalid=3
routes_total=487
boundary_violations=0
files=1594
lines_total=258808
lines_code=218227
duration_ms=22094
```

说明：
- `repo` 取仓库根目录名（非绝对路径），保证跨机器输出一致；
- `duration_ms` 取真实耗时（`--deterministic` 只影响落盘产物，不影响 stdout 摘要）；
- `--summary-only` 会抑制 `[dev-insight] 仓库: ...` 提示行，确保 stdout 纯净可解析。

实现位置：`cli.py` 在 `run()` 末尾、`written` 落盘之前判断，命中则 `print` 后 `return 0`。

---

## 3. 阶段 B：AST 缓存与增量分析

### 3.1 问题分析

`core_analyzer` 对每个核心模块调用一次 `iter_repo_files(cfg)`（全仓 `os.walk`），`plugin_analyzer` 对每个插件再调一次，`interaction_analyzer` 与 `deep_analyzer` 又各调一遍。实测 12 个核心模块 + 40 个插件 ≈ 55 次全仓遍历，是 22 秒耗时主因。

### 3.2 改造方案

**新增 `vr_insight/cache.py`**：

```python
# -*- coding: utf-8 -*-
"""AST 解析结果缓存：以 (mtime_ns, size) 作签名，跨运行复用解析结果。"""
from __future__ import annotations

import json
from pathlib import Path

CACHE_DIRNAME = ".insight_cache"
CACHE_FILE = "ast_cache.json"
CACHE_VERSION = 1


def _signature(fpath: Path) -> str:
    st = fpath.stat()
    return f"{st.st_mtime_ns}:{st.st_size}"


def cache_path(out_dir: Path) -> Path:
    return out_dir / CACHE_DIRNAME / CACHE_FILE


def load_cache(out_dir: Path) -> dict:
    p = cache_path(out_dir)
    if not p.exists():
        return {}
    try:
        data = json.loads(p.read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError):
        return {}
    if data.get("cache_version") != CACHE_VERSION:
        return {}
    return data.get("entries", {})


def save_cache(out_dir: Path, entries: dict) -> None:
    p = cache_path(out_dir)
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(json.dumps({"cache_version": CACHE_VERSION, "entries": entries},
                            ensure_ascii=False), encoding="utf-8")
```

**在 `cli.py` 中接线**（`parse_cache` 从裸 dict 升级为带预热的 dict）：

```python
cache_entries = load_cache(cfg.out_dir)
parse_cache: dict = {}

def _get_entry(rel: Path, fpath: Path) -> dict:
    """统一入口：命中缓存则复用，否则解析并登记。"""
    key = str(rel)
    sig = _signature(fpath)
    hit = cache_entries.get(key)
    if hit and hit.get("sig") == sig:
        entry = hit["entry"]
    else:
        entry = parse_python_file(fpath, key)
        cache_entries[key] = {"sig": sig, "entry": entry}
    parse_cache[key] = entry
    return entry
```

**关键：缓存只覆盖 AST 解析，不覆盖分析结论。** 各 analyzer 的 `parse_cache.get(str(rel)) or parse_python_file(...)` 调用点保持不变（缓存已在 `run()` 阶段预热 `parse_cache`），因此**本阶段对 5 个 analyzer 零改动**，只需在 `run()` 开头预热、结尾 `save_cache`。

预热：遍历一次 `iter_repo_files(cfg)`，对 `.py` 文件调 `_get_entry`。
收尾：`save_cache(cfg.out_dir, cache_entries)`。

**预期收益**：首次运行不变（约 22s），二次运行仅解析变更文件，实测目标 < 3s。

### 3.3 缓存失效与清理

- 签名含 `mtime_ns + size`：文件被 `git checkout` 切回旧版本时 mtime 会变，自动失效，安全。
- 提供 `--no-cache` 开关跳过读写。
- 缓存目录位于 `out/.insight_cache/`，而 `config.DEFAULT_EXCLUDE_DIRS` 已含 `dev_insight`，不会污染统计。

---

### 3.4 实测结果（阶段 B 已完成）

测试环境：合成仓库 331 个 `.py`（30 个核心 + 30 插件 × 10 文件，约 6.6 万行），仅输出 JSON。

| 验收项 | 结果 |
|---|---|
| 冷启动（无缓存） | 6.53s |
| 热运行（命中缓存，稳定态） | 3.68 ~ 3.92s |
| 加速比 | 约 1.7x（**B+ 优化前**） |
| cold / warm / warm2 产物 | 逐字节相同（缓存不改变输出） |
| `--no-cache` vs 有缓存 | 逐字节相同（开关不影响结果） |
| 失效正确性 | 改 1 个文件 → 对应插件 `loc` 精确 +40 行 |
| 缓存体积 | 1.31 MB / 331 个 .py |

**附带修复**：预热使 `core.modules[].package_docstring` 由恒为 `null` 恢复为真实值（见 3.6）。

### 3.5 B+ 追加优化（已完成，达成 3x 目标）

用户批准后实施。测试环境同上（332 个 `.py`，含 `plugin_manager/{base,manager,discovery}.py`）。

| 优化项 | 改动文件 | 做法 |
|---|---|---|
| ① 遍历索引缓存 | `fs_scan.py` | `iter_repo_files` 首次执行 `os.walk` 后按 `repo_root` 缓存结果，后续调用直接 `yield from`。实测 10 次调用只触发 **1 次** `os.walk`。 |
| ② LOC 复用 | `fs_scan.py` + `py_ast.py` | `scan_overview(cfg, parse_cache)` 对 `.py` 直接取 `entry["loc_total"]/["loc_code"]`，不再二次读全文；并把两处的行数口径统一到新函数 `count_lines()`。 |
| ③ 缓存键规范化 | `core_analyzer.py` + `plugin_analyzer.py` | 见 3.6，这是缓存命中率的直接阻塞点。 |

**最终实测**：

| 指标 | B（优化前） | B+（优化后） | 变化 |
|---|---:|---:|---|
| 冷启动 | 6.53s | **4.09s** | −37%（索引缓存对冷路径同样生效） |
| 热运行（中位数） | 3.75s | **1.08s** | −71% |
| **加速比** | 1.7x | **3.77x** | 达成 3x 目标 |
| `--no-cache` | — | 4.20s | 与冷启动一致 |
| 产物一致性 | 通过 | 通过 | cold==warm==`--no-cache`，逐字节相同 |
| 失效正确性 | +40 行 | +40 行 | 保持 |

**口径统一验证**（复用 `parse_cache` 的前提）：

- 对全部 `.py` 逐文件比对"仅排除 `#`"与"排除 `#`/`//`"两种口径，差异数 **0**；
- `scan_overview(cfg)` 与 `scan_overview(cfg, parse_cache)` 输出完全相等（`LOC_REUSE_IDENTICAL = True`）。

**未采纳：缓存瘦身**。实测 1.9MB 缓存的 `json` 读写合计 180ms，占热运行 1080ms 的 **17%**；改用紧凑 `separators` 仅减 12% 体积（约省 20ms，<2%），却牺牲缓存文件的可 grep 性。**收益不足，暂不实施**。

### 3.6 缓存键规范化（B+ 期间定位的既有缺陷）

**现象**：B+ 优化后热路径仍有 31 次 `ast.parse`，与 `plugin_manager/` 下的 `.py` 数量完全吻合。

**根因**：`_analyze_plugin_system` 用硬编码 POSIX 风格键查询 `parse_cache`：

```python
entry = parse_cache.get("plugin_manager/base.py")   # 正斜杠
rel = "plugin_manager/" + py.name
```

而 `cli.py` 预热与其余分析器统一使用 `str(Path(...))`，在 Windows 上为 `plugin_manager\base.py`（反斜杠）。**两者永不相等**，导致：

1. `plugin_manager/` 下每个文件每次运行都被重复解析；
2. 这些文件的 AST 缓存**永不命中**，缓存中还混入了正斜杠键的重复条目；
3. 同一文件在 `parse_cache` 里存在两份 `entry`，其 `path` 字段风格不一致。

同类问题亦存在于 `plugin_analyzer` 的 `plugins/_base` 框架资源处（`f"{PLUGINS_DIR}/_base/{py.name}"`）。

**修复**：新增 `core_analyzer._pm_key(name)` 统一定为 `str(Path("plugin_manager") / name)`；`plugin_analyzer` 改用 `str(Path(PLUGINS_DIR) / "_base" / py.name)`。输出字段（`file`、`module` 等）**保持 POSIX 风格不变**，故产物零变化。

**验证**：修复前后在同一仓库上运行，`--deterministic` 产物**逐字节相同**（65,394 B）；热路径 `compile` 调用由 36 次降至 7 次（剩余为启动期 import），`_extract_routes` 完全退出 profile Top 8。

> 注：这是**既有缺陷**，在引入 AST 缓存前无感（原本每次都全量解析）。它同时解释了为什么 B 阶段实测只有 1.7x——核心目录始终无法命中缓存。

### 3.7 遗留的既有缺陷（未修复，待决策）

`core_analyzer.analyze_core` 中 `init_entry = parse_cache.get(str(init_rel))` 位于填充 `parse_cache` 的循环**之前**，导致 `package_docstring` 恒为 `None`，并使未在 `KNOWN_CORE_MODULES` 中的模块 `description` 退化成"自动识别的 Python 代码模块"。

阶段 B 通过"无论是否启用缓存都完整预热"已规避该问题（实测 `package_docstring` 恢复为 `'插件框架'`），**未改动 analyzer**。若要彻底修复，把该读取移到填充循环之后即可（约 3 行）。

## 4. 阶段 C：`--fail-on` 门禁退出码

### 4.1 退出码约定

| 码 | 含义 | 触发条件 |
|---|---|---|
| 0 | 通过 | 无命中门禁项 |
| 1 | **门禁失败** | `--fail-on` 指定的检查项命中 |
| 2 | 参数错误 | argparse 错误 / `--repo` 定位失败（已有） |
| 3 | 运行异常 | 捕获到未预期异常（替换现在的 `return 1`） |

### 4.2 检查项定义

```python
GATE_CHOICES = (
    "manifest-invalid",     # 存在 manifest 校验失败的插件
    "boundary-violation",   # 核心模块直接 import 业务插件
    "plugin-cycle",         # 插件间存在循环依赖（依赖阶段 G 的环检测）
    "route-unprefixed",     # 插件路由未遵循 /admin/<identifier> 前缀惯例
    "file-too-large",       # 单文件代码行超过 --max-file-lines（默认 2000）
    "all",                  # 以上全部
)
```

### 4.3 实现骨架（新增 `vr_insight/gate.py`）

```python
@dataclass
class GateResult:
    name: str
    passed: bool
    hits: list[dict]        # 每条含 file / detail
    threshold: str | None = None


def evaluate(gates: set[str], data: dict, cfg) -> list[GateResult]:
    results = []
    if "manifest-invalid" in gates or "all" in gates:
        hits = [{"plugin": p["identifier"],
                 "detail": "; ".join(p["manifest_errors"])}
                for p in data["plugins"]["items"] if not p["manifest_valid"]]
        results.append(GateResult("manifest-invalid", not hits, hits))
    # …其余检查项同理，数据均已存在于 data 中，无需重新扫描
    return results
```

`cli.py` 末尾：

```python
if args.fail_on:
    results = evaluate(set(args.fail_on.split(",")), data, cfg)
    failed = [r for r in results if not r.passed]
    for r in results:
        print(f"[gate] {'PASS' if r.passed else 'FAIL'} {r.name} "
              f"({len(r.hits)} hits)")
    if failed:
        return 1
```

### 4.4 落地建议（避免 CI 被误伤）

- 首个迭代只启用 `manifest-invalid` 与 `boundary-violation` 两项，其余默认关闭。
- 提供 `--fail-on-warn`：只打印不返回非零，用于灰度观察一周。

### 4.5 实现说明与实测结果（阶段 C 已完成）

**实现要点（与 4.1–4.4 的差异）**：

| 项 | 方案 | 实际实现 | 理由 |
|---|---|---|---|
| 默认行为 | 首迭代默认启用 manifest-invalid + boundary-violation | **默认全部不检查**，须显式 `--fail-on` | 更保守：不传参数时行为与历史版本完全一致，CI 接入零风险 |
| `--fail-on` 校验 | 未明确 | 非法项返回退出码 **2** 并列出可选项 | 与 argparse 参数错误同码，便于 CI 区分"配置错"与"门禁失败" |
| 门禁输出位置 | stdout | stdout；`--summary-only` 时改走 **stderr** | 保证阶段 A 建立的"stdout 恰好 11 行 key=value"契约不被污染 |
| `file-too-large` 数据来源 | 称"数据均已存在于 data 中" | **需按阈值重新遍历统计** | `overview.top_files` 仅保留 Top 25，不足以做全量判定。故按需遍历（仅该项被选中时才付出代价，且复用遍历索引缓存） |
| `route-unprefixed` 非字面量前缀 | 未明确 | 遇到变量/常量引用**跳过判定** | 静态无法确定实际路径；宁可漏报，不可误伤 |
| 门禁结果写入 JSON | 未提及 | **不写入** | 保持产物 schema 稳定；结构化输出留待阶段 E 的 `query` |

**新增文件**：`vr_insight/gate.py`（`GateResult` 数据类 + 5 个 `_check_*` + `expand()` + `evaluate()`；环检测为 DFS 三色标记，环路径旋转到最小元素开头去重）。

**实测（构造含 5 类违规的合成仓库）**：

| 场景 | 期望 | 实测 |
|---|---|---|
| 不传 `--fail-on` | rc=0，无 gate 行 | 通过 |
| 5 个检查项分别单独启用 | 各自 rc=1 且定位准确 | 通过（含环 `plug_a → plug_b → plug_a`、prefix `/wrong_path`、201 行 > 100） |
| `--fail-on all` | rc=1，5 项全 FAIL 且各附 1 条明细 | 通过 |
| `--fail-on-warn` | rc=0 但仍打印 FAIL | 通过 |
| 非法门禁项 | rc=2 + 可选列表 | 通过 |
| `--summary-only` + `--fail-on` | rc=1，stdout 恰好 11 行无 gate 行，gate 走 stderr | 通过 |
| 干净仓库 `--fail-on all` | rc=0，5 项全 PASS | 通过 |
| 内部异常 | rc=3 | 通过（注入 `render_tree` 抛错验证） |

环检测单元验证：三角环、自环、双向环去重（1 个）、双独立环（2 个）、菱形无环（0 个）——均正确。

**`main()` 退出码已由 1 改为 3**（运行异常），与门禁失败（1）、参数错误（2）分离。

---

## 5. 阶段 D：`snapshot` 与 `diff`

### 5.1 快照结构（精简，不含 tree 与路由明细）

```python
def build_snapshot(data: dict) -> dict:
    return {
        "schema_version": SCHEMA_VERSION,
        "snapshot_version": "1.0",
        "generated_at": None,          # 快照默认确定性
        "totals": {
            "files": data["overview"]["total_files"],
            "lines_total": data["overview"]["total_lines"],
            "lines_code": data["overview"]["total_code_lines"],
        },
        "core": {
            m["name"]: {"loc_code": m["loc_code"], "routes": m["route_count"]}
            for m in data["core"]["modules"]
        },
        "plugins": {
            p["identifier"]: {
                "version": p["version"],
                "loc_code": p["loc_code"],
                "routes": p["route_count"],
                "manifest_valid": p["manifest_valid"],
                "manifest_errors": p["manifest_errors"],
                "depends_on": sorted((p["depends_on"] or {}).keys()),
            }
            for p in data["plugins"]["items"]
        },
    }
```

体积实测估算：40 插件 × 约 8 字段 ≈ 6KB，可安全进 Git。

### 5.2 子命令设计（兼容现有用法）

```python
sub = ap.add_subparsers(dest="cmd")     # 不加 required=True，保持无子命令时走原路径

sp = sub.add_parser("snapshot", help="生成/保存基线快照")
sp.add_argument("--save", metavar="NAME", help="保存为 history/<NAME>.json，默认用日期")
sp.add_argument("--stdout", action="store_true", help="输出到 stdout 而不落盘")

dp = sub.add_parser("diff", help="与基线快照对比")
dp.add_argument("baseline", help="基线名称或 JSON 路径")
dp.add_argument("--top", type=int, default=10, help="LOC 增量 Top N")
dp.add_argument("--format", choices=("md", "json"), default="md")
```

### 5.3 diff 输出内容

1. **插件变更**：新增 / 删除 / 版本变化（`1.2.0 → 1.3.0`）
2. **LOC 增量 Top N**：按 `loc_code` 差值降序，插件与核心模块分列
3. **路由变更**：`+N / -M` 条
4. **manifest 新增错误**：本次出现、基线中不存在的错误
5. **依赖变更**：`depends_on` 增删

输出示例（Markdown）：

```markdown
# Insight Diff · baseline=2026-08-10 → current

## 插件变更
- ➕ 新增 `ros_bridge` (1.0.0, 3,558 行)
- 🔼 `stock_analysis` 2.1.0 → 2.2.0 (+14,770 行)

## LOC 增量 Top 10
| 目标 | 基线 | 当前 | 增量 |
|---|---:|---:|---:|
| plugins/stock_analysis | 21,340 | 36,110 | +14,770 |
```

> 这份输出即《代码膨胀归因报告》的自动化版本——目前那份报告依赖人肉 `git log` 统计，做成内置命令后可在 CI 中每日自动生成。

### 5.4 实现说明与实测结果（阶段 D 已完成）

**交付**：新增 `vr_insight/snapshot.py`（快照构建 / 存取 / 差异计算 / Markdown 渲染），改造 `cli.py`（抽出 `_setup` / `_analyze` 两个公共函数，三个命令复用同一条分析路径）。

**CLI 设计**：

```bash
insight snapshot --save baseline          # 落 out/history/baseline.json（默认名为当天日期）
insight snapshot --stdout                 # 打印到 stdout，不落盘
insight diff baseline                     # Markdown 变更报告
insight diff baseline --format json --top 20   # 结构化输出，供 Agent 解析
```

子命令通过 `add_subparsers(dest="cmd")` 接入且**不设 `required=True`**，无子命令时走原全量分析路径，历史用法零变化。

> **一个必须避开的 argparse 陷阱**：子命令复用的公共参数（`--repo` / `--out` / `--quiet` / `--deterministic` / `--no-cache` / `--tree-depth`）默认值一律设为 `argparse.SUPPRESS`。否则子解析器的 `default=None` 会覆盖主解析器已解析出的同名值，导致 `insight.py --repo X snapshot` 拿不到仓库路径。已实测两种参数顺序（`--repo` 在前 / 在后）产物完全一致。

**diff 输出 schema**（`--format json`）：

```jsonc
{
  "baseline": "baseline", "current": "current",
  "diff": {
    "has_changes": true,
    "totals":  {"files": {"base":..,"cur":..,"delta":..}, "lines_total": {...}, "lines_code": {...}},
    "plugins": {"added": [{identifier,version,loc_code,routes}],
                "removed": [...],
                "version_changed": [{identifier,base_version,cur_version,loc_delta,route_delta}]},
    "core":    {"added": [...], "removed": [...]},
    "loc":     {"changed_count": 4, "top": [{target,base,cur,delta}]},
    "routes":  [{target,base,cur,delta}],
    "manifest":{"new_errors": [{plugin,errors}], "fixed": [{plugin,errors}]},
    "deps":    [{plugin,added,removed}]
  }
}
```

**实测结果**（合成仓库，30 插件 + 2 核心模块，334 个 .py）：

| 验收项 | 结果 |
|---|---|
| 快照体积 | **5,561 B**（30 插件）→ 40 插件实测量级约 7KB，可安全进 Git |
| 快照确定性 | 同状态两次生成**逐字节相同** |
| 无变更 diff | 输出「无差异」，rc=0 |
| 五类变更识别 | 新增 / 删除 / 版本变化 / LOC 变动 / 路由变更 / manifest 新错误 / 依赖变更 —— 全部命中 |
| `snapshot` 耗时 | 1.00s（334 文件，命中缓存） |
| `diff` 耗时 | 0.96s（含一次全量分析） |
| 向后兼容 | 无子命令的全量分析、`--summary-only` 11 行、门禁 rc、缓存加速 —— 回归全通过 |

**对方案的四处主动偏离**：

1. **LOC 变动按 `|增量|` 降序**，而非方案所写的「增量降序」。理由：插件被整体删除是幅度最大的变动，按增量降序会把它挤到列表末尾甚至被 Top N 截断。表格中 `Δ` 列带符号，涨/缩一眼可辨。
2. **快照恒为确定性**（`generated_at` 恒为 `None`），时间维度由文件名承载（`--save` 默认当天日期）。否则同一天两次快照无法比对，违背「基线」的语义。
3. **补充了方案未列的三项**：manifest **已修复**错误（`fixed`）、核心模块增删、路由变更独立成节。仅报「新增错误」会让修复动作在报告里消失，无法闭环。
4. **基线解析支持文件路径**（`insight diff D:\tmp\base.json`），并对 `--save` 做文件名规整：`../../evil` → `.._.._evil.json`，阻断路径穿越。

**错误处理**（统一 rc=2）：基线不存在时列出 `history/` 下可用基线；快照 JSON 损坏、缺 `plugins`/`totals` 段、`snapshot_version` 不兼容均显式报错而非静默错算。

**已知限制**：

- `diff` 每次需跑一次全量分析（当前约 1s），不做「只读快照不扫描」的短路——因为必须与当前真实状态比对，而当前状态只能扫出来。
- 快照不含路由明细，因此只能回答「某插件路由 +3 条」，不能回答「新增了哪 3 条」。定位明细仍需 `--plugin` 深挖。
- 只回答「变了什么」，不回答「为什么变」。归因需要 git 历史与人的判断，不在本模块职责内。

---

## 6. 阶段 E：面向 Agent 的消费契约

### 6.1 `context --for` 按需切片

复用现有 `report_ai.py` 的渲染骨架，把"全局插件清单"替换为"指定目标详情"：

```bash
insight context --for plugin:stock_analysis      # 单插件上下文（约 2-3KB）
insight context --for module:plugin_manager      # 单核心模块上下文
insight context --depth brief|normal|full        # 颗粒度
```

`brief` 版内容（约 40 行）：目标 manifest 摘要、路由表、BasePlugin 子类、依赖与被依赖、manifest 校验结果。
`full` 版追加：全部类/方法签名、导入聚合。

### 6.2 `query` 定向查询

不做通用表达式解析器（易变成维护负担），实现**受限查询**：

```bash
insight query --select plugins[].identifier,version,route_count \
              --where manifest_valid=false \
              --format json|table
```

实现要点：

- `--select`：点分/方括号路径，用 `re` 拆分后递归取值，不支持过滤表达式。
- `--where`：仅支持 `key=value` 与 `key!=value`，多个用逗号分隔取 AND。
- 输出默认 `json`（供 Agent 解析），`table` 供人看。
- **路径语法严格受限于三式**（点分字段 / `[]` / `[n]`）：非法下标（`[abc]`、`[1.5]`）、不配对括号、缺失点分隔等一律抛 `QueryError` 并归入 **rc=2**，绝不静默重解释成"看起来合理实则为空"的错误路径（后者对 Agent 最难排查）。路径语法在**全量分析前**统一预检（含 `--select` 字段、`--where` 的 key，以及无 `--from` 时的 scope 推断），保证参数错误稳定落在 rc=2 而非被顶层兜底误判为 rc=3。

### 6.3 结构化错误

改造 `cli.py` 的顶层兜底：

```python
except SystemExit:
    raise
except Exception as e:
    payload = {"error": {"type": type(e).__name__,
                         "message": str(e),
                         "hint": _hint_for(e)}}
    print(json.dumps(payload, ensure_ascii=False), file=sys.stderr)
    return 3
```

`_hint_for()` 针对已知错误给可操作建议（如仓库定位失败 → 提示 `--repo` 用法）。

### 6.4 验收与两处缺陷修复（阶段 E 收尾）

阶段 E 实施完成后，在合成仓库（2 插件 / 3 核心模块）上验收，发现并修复两处缺陷，附带一处 latent 笔误修复：

| 编号 | 现象 | 根因 | 修复 |
|---|---|---|---|
| E-1 | `context --for module:<函数型模块> --depth brief` 几乎为空（仅 标识表） | 模块切片在 brief 下只渲染 关键类（类为空则不显示），公开函数与"被依赖"被 `depth != "brief"` 门控，无类的模块切片空荡 | `report_ai.py`：为模块新增"公开函数"小节（brief/normal 各限 8/20 条，full 沿用原专属小节避免重复）；模块"被依赖"小节取消 brief 门控；`_CLASS_CAP.brief` 由 3 提至 5 |
| E-2 | `query --select plugins[].routes[].rule` 渲染为列名 `routes.[].rule`（多出一个 `.`） | `query.py` 用 `".".join(steps)` 拼接列名，`[]`/`[n]` 步骤前被强行插入 `.` | `query.py` 新增 `_col_name()`：bracket 步骤前不加 `.`，`routes[].rule` / `routes[0].rule` 原样呈现，且拼接结果仍是合法路径可供 `get_path` 复用 |
| E-3 | `context --for module:X --depth full` 抛 `NameError: _FUNC_CAP` | 笔误引用了未定义的 `_FUNC_CAP`（应为命名修正遗漏），full 切片走到该分支即崩 | 正式定义 `_FUNC_CAP`（`brief:8 / normal:20 / full:10**6`），移除未被引用的 `_FILE_CAP` |
| E-4 | 非法字段路径被静默重解释成错误路径，或误判为 rc=3 | `query.py` 的 `re` 拆分把 `[abc]`、`[1.5]`、`routes[]rule` 等静默拆成合法步骤 → 输出"看似合理实则为空"；部分路径在顶层兜底被归为 rc=3 内部错误 | `query.py` 新增 `_PATH_FULL` 全路径形态校验 + 公开 `parse_query_path()`，契约外语法一律抛 `QueryError`；`cli.py._cmd_query` 把 scope 推断与 select/where 路径预检移入 try 块，使所有参数错误稳定归 **rc=2** |

验收命令（合成仓库，均已通过）：

```bash
insight context --for module:shared --depth brief        # 现含 标识 + 被依赖 + 公开函数（此前仅 标识）
insight query --select plugins[].routes[].rule --from plugins --format table   # 列名 routes[].rule（此前 routes.[].rule）
insight context --for module:orchestrator --depth full   # 不再 NameError，公开函数经专属小节呈现
insight query --select plugins[].routes[0].rule          # 列名 routes[0].rule
insight query --select plugins[].routes[abc].rule        # 语法错误 → rc=2（而非静默空结果 / rc=3）
insight query --select identifier --where 'routes[1.5].rule=/x'   # --where 非法路径 → rc=2
```

---

## 7. 阶段 F：`rules/` 规则引擎（设计完成，实施排最后）

### 7.1 数据结构

```python
@dataclass
class Finding:
    rule_id: str        # 如 "SEC001"
    severity: str       # error | warning | info
    category: str       # spec | security | architecture | complexity
    target: str         # 插件 identifier / 模块名 / 文件路径
    file: str | None
    line: int | None
    message: str
    evidence: str       # 触发依据：命中的代码片段或字段值
```

输出 `data["findings"]`：

```json
{
  "findings": {
    "schema_version": "1.0",
    "summary": {"error": 2, "warning": 7, "info": 3},
    "items": [ /* Finding 列表，按 severity → rule_id → target 排序 */ ]
  }
}
```

### 7.2 规则清单

| rule_id | 类别 | 级别 | 判定依据 | 数据来源（现已具备） |
|---|---|---|---|---|
| `SPEC001` | 规范 | error | `agent_role` 不在核心角色集合内 | manifest |
| `SPEC002` | 规范 | error | `capabilities` 为空 | manifest |
| `SPEC003` | 规范 | warning | 插件缺 `i18n/` 目录 | `i18n_locales` |
| `SPEC004` | 规范 | warning | 插件缺 README/文档 | `docs` |
| `SPEC005` | 规范 | error | `version` / `min_app_version` 非语义化 | 已有校验逻辑 |
| `SEC001` | 安全 | error | 源码命中密钥/token 硬编码正则 | 需新增文本扫描 |
| `SEC002` | 安全 | warning | 出现 `eval(` / `exec(` | 需 AST 扩展 |
| `SEC003` | 安全 | warning | `subprocess(..., shell=True)` | 需 AST 扩展 |
| `SEC004` | 安全 | warning | SQL 字符串拼接（f-string 含 SELECT/INSERT） | 需文本扫描 |
| `ARCH001` | 架构 | error | 核心模块直接 import 业务插件 | `boundary_observations` |
| `ARCH002` | 架构 | error | 插件间循环依赖 | 需对 `plugin_to_plugin` 做环检测 |
| `ARCH003` | 架构 | error | 插件私有连接池（直连 `psycopg2.connect` / `create_engine`） | 需 AST 扩展 |
| `ARCH004` | 架构 | warning | 插件路由未遵循 `/admin/<identifier>` 前缀 | `routes` |
| `CMP001` | 复杂度 | info | 单文件代码行 > 阈值（默认 2000） | `top_files` |
| `CMP002` | 复杂度 | info | 单函数体 > 阈值（默认 120 行） | 需 AST 扩展 |

**依赖新增能力的只有 5 条**（SEC001-004、ARCH003、CMP002），其余 8 条用现有数据即可实现——建议首轮只上这 8 条，成本极低。

### 7.3 目录结构

```
vr_insight/rules/
    __init__.py        # 注册表：RULES = [rule_fn, ...]
    _types.py          # Finding dataclass + severity 常量
    spec.py            # SPEC001-005
    security.py        # SEC001-004
    architecture.py    # ARCH001-004
    complexity.py      # CMP001-002
```

每条规则签名统一：`def rule(ctx: AnalysisContext) -> list[Finding]`，由 `__init__.py` 汇总排序。提取与判断解耦，新增规则不改任何 analyzer。

---

## 8. 逐文件改动清单

| 文件 | 阶段 | 改动类型 | 说明 |
|---|---|---|---|
| `vr_insight/__init__.py` | A | 修改 | 新增 `SCHEMA_VERSION = "1.0"` 与演进规则注释 |
| `vr_insight/cli.py` | A–E | 修改 | meta 构造抽函数；新增 `--deterministic` / `--summary-only` / `--fail-on` / `--no-cache`；新增子命令解析（兼容无子命令）；结构化错误 |
| `vr_insight/cache.py` | B | **新增** | 缓存读写与签名 |
| `vr_insight/gate.py` | C | **新增** | 门禁检查项 |
| `vr_insight/snapshot.py` | D | **新增** | 快照构建 + diff 计算 |
| `vr_insight/report_ai.py` | E | 修改 | 抽取 `context --for` 渲染分支 |
| `vr_insight/query.py` | E | **新增** | 受限查询 |
| `vr_insight/rules/` | F | **新增目录** | 规则引擎（6 个文件） |
| `vr_insight/report_md.py` | F | 修改 | 新增 findings 章节 |
| `vr_insight/report_html.py` | F | 修改 | 新增 findings 表格 |
| `vr_insight/config.py` | A/C/D | 修改 | 新增阈值字段（`max_file_lines` 等） |
| `insight.py` | — | 不变 | 入口保持 24 行 |
| 5 个 analyzer | — | 不变 | 阶段 A–E 全程零改动 |

**刻意不动的部分**：`interaction_analyzer.py`、`standards_extractor.py`、`deep_analyzer.py`。它们已在上一轮核查中确认职责清晰，扩展通过新增模块完成，避免回归风险。

> **B+ 已对上述清单做出例外**（用户单独批准）：`fs_scan.py`（遍历索引 + LOC 复用）、`py_ast.py`（LOC 口径统一）、`core_analyzer.py` 与 `plugin_analyzer.py`（缓存键规范化）。其中键规范化是既有缺陷修复，无此修复 AST 缓存对核心目录永不命中。理由与验证见 3.5 / 3.6。

---

## 9. 实施顺序与验收标准

| 阶段 | 内容 | 验收命令 | 验收标准 |
|---|---|---|---|
| A | 确定性 + schema + 摘要 | `python insight.py --repo X --deterministic --only json` 跑两次后 `diff` | 两份 JSON 逐字节相同 |
| A | summary-only | `python insight.py --repo X --summary-only` | stdout 恰好 11 行 `key=value`，无提示行、无文件落盘 |
| B | 缓存 | 连续跑两次计时 | 二次耗时 < 首次的 30% |
| B | 缓存正确性 | 改动一个 .py 后再跑 | 该文件的变更被正确反映（缓存失效生效） |
| C | 门禁 | `--fail-on manifest-invalid` | 存在非法 manifest 时 `echo $?` 为 1，否则为 0 |
| D | 快照 | `insight snapshot --save baseline` | 生成 `out/history/baseline.json`，体积 < 20KB |
| D | diff | `insight diff baseline` | 输出含插件变更与 LOC Top N |
| E | context | `insight context --for plugin:stock_analysis` | 输出 < 4KB 且含该插件路由表 |
| E | query | `insight query --select plugins[].identifier --where manifest_valid=false` | 返回合法 JSON 数组 |
| F | 规则 | `--fail-on findings` | findings 按 severity 正确汇总并渲染进 MD/HTML |

**回退策略**：每个阶段独立提交，任一阶段验收不通过即停止，后续阶段不启动。阶段 A–E 对现有 5 个 analyzer 零改动，回退成本接近零。

---

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 缓存导致结果陈旧 | 分析结论错误 | 签名含 `mtime_ns`；提供 `--no-cache`；首次使用默认回源 |
| `--fail-on` 误报卡死 CI | 团队抵触 | 首轮只开 2 项；提供 `--fail-on-warn` 灰度一周 |
| 快照塞进 Git 变大 | 仓库膨胀 | 快照仅存聚合值（< 20KB）；`history/` 保留最近 30 份 |
| 子命令改造破坏现有用法 | 现有脚本失效 | `add_subparsers` 不设 `required=True`，无子命令走原路径 |
| Windows 路径大小写 | 缓存键错配 | 缓存键统一用 `rel.as_posix().lower()` |
| 规则误报过多 |  findings 被无视 | 规则分级；`info` 级不计入门禁；规则可 `--disable-rule` |

---

## 11. 明确不做的事

1. **工具内接入 LLM** —— 破坏确定性，引入密钥与成本，与定位冲突。
2. **自动修复代码** —— 工具只读，不做写操作。
3. **通用表达式查询语言** —— `--where` 只支持等值比较，避免自造 DSL 的维护负担。
4. **引入前端框架 / mermaid CDN** —— 可视化坚持 Python 生成 SVG 或内联 JSON 的单文件 HTML，保证离线可用。
5. **改动 5 个 analyzer** —— 本轮扩展全部通过新增模块完成。

---

*文档状态：待确认。确认后按阶段 A → F 顺序实施，每个阶段单独报批。*
